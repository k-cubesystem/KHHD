# 신당 음원 라이선스 대장 (CREDITS)

이 폴더의 음원 파일 출처·라이선스·확인일을 기록한다. **라이선스가 불확실한 파일은 절대 넣지 않는다.**
파일이 없으면 앱은 자동으로 오실레이터 합성음으로 폴백하므로, 수급과 무관하게 배포는 안전하다.

## 파일 규약 (`components/shrine/scene/useShrineAudio.ts`)

- **효과음(FX)**: `fx-{soundKey}.mp3` — soundKey ∈ `moktak`(목탁) · `chime`(풍경) · `bell`(종) · `water`(낙수) · `crackle`(촛불) · `bara`(바라)
  - 있으면 Web Audio 버퍼로 재생, 없으면 합성 폴백. 짧게(≤2초) 권장.
- **배경음(BGM)**: `bgm-{theme}.mp3` — theme ∈ 16테마 전부(아래 대장 참조)
  - 있으면 `HTMLAudioElement`(loop, 음량 0.25)로 재생, 없으면 절차 국악 앰비언트 폴백.

## 수급 가이드 (상업 사용 가능 기준만)

| 소스                           | 라이선스           | 상업            | 용도                      | 의무                               |
| ------------------------------ | ------------------ | --------------- | ------------------------- | ---------------------------------- |
| 국립국악원 gugak.go.kr         | 공공누리 1유형     | ✅              | BGM 1순위(정악·산조·풍류) | **출처표시 필수** → 이 파일에 기재 |
| 공유마당 gongu.copyright.or.kr | 건별(CC0/CC-BY 등) | ✅              | 전통음악                  | 건별 라이선스 확인                 |
| Pixabay pixabay.com            | Pixabay License    | ✅(재배포 금지) | 효과음                    | 표기 불요(권장)                    |
| freesound.org (CC0 필터)       | CC0                | ✅              | 목탁·풍경·종 실녹음       | CC0 만 사용                        |
| FreePD freepd.com              | CC0                | ✅              | 폴백                      | —                                  |

**제외**: Suno/Udio 무료 티어(상업 불가), YouTube Audio Library(유튜브 외 제약).

## 현재 상태 (2026-08-11)

**BGM 16트랙 = 자체 합성(제3자 권리 0). 효과음(FX) 수급 파일은 여전히 0건.**

CEO 결정으로 Suno 국악 선율은 배제하고 **코드 합성 싱잉볼**을 채택했다. 외부 음원·생성 모델을
쓰지 않으므로 **라이선스 리스크·과금이 없고, 재생성이 결정론적**이다(같은 시드 → 바이트 동일).

## BGM 대장 — 16트랙 (자체 저작)

| 항목     | 내용                                                                                    |
| -------- | --------------------------------------------------------------------------------------- |
| 파일     | `bgm-{theme}.mp3` × 16 · 각 704KB · 합계 11.0MB                                         |
| 제작     | 해화당 자체 — `scripts/media-assets/shrine-ambience.mjs` (DSP 합성, 생성 API 0회)       |
| 사양     | 60.0033초 seamless loop · 44.1kHz 스테레오 · MP3 96kbps CBR · Xing/LAME 게이트리스 태그 |
| 라이선스 | 해화당 소유 (제3자 소재·샘플·생성 모델 미사용)                                          |
| 확인일   | 2026-08-11                                                                              |

재생성: `node scripts/media-assets/shrine-ambience.mjs --all` · 검증만: `--check` · 루트표: `--roots`
⚠️ 중간 wav 는 `D:/anti/media/out/shrine-bgm/` 로 나간다(C: 여유 없음). 최종 mp3 만 이 폴더에 남는다.

## FX 기재 양식 (파일 추가 시 아래 표에 한 줄)

| 파일                 | 제목/원저작자 | 출처(URL)      | 라이선스 | 확인일     |
| -------------------- | ------------- | -------------- | -------- | ---------- |
| _(예)_ fx-moktak.mp3 | 목탁 1타      | freesound.org/ | CC0      | 2026-07-21 |
