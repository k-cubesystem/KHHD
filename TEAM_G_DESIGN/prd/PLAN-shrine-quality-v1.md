# PLAN-shrine-quality-v1 — 신당 품질 종합 기획 (야간 감사 → 실행)

버전: v1.0 확정(감사 4트랙 수합·야간 실행 반영) | 작성: Fable | 날짜: 2026-07-31 심야
지시: CEO 2026-07-31 01:21 — "기획대로 잘 설계되고 있는지 / 개선할 점 / 디버깅할 곳 / 보안 이슈 /
디자인 퀄리티 상향 기획 — 전부 분석해서 기획서 작성하고 그대로 작업시켜줘. 아침에 볼 것."

---

## 0. 결론 먼저 (아침 3분 요약)

**"기획대로 가고 있는가" — 예. 코드 기반은 건강하다(A1 P0 0건).** 그러나 게이트(테스트 1,275개
전부 초록)가 **구조적으로 못 잡는 무증상 사망 3계열**이 실브라우저·라이브 DB 감사에서 나왔고,
전부 야간에 수복했다:

1. **보안 P0 3건** (A3) — 유료 신위·테마 무료 착용 뒷문 / 무료 상한 동시요청 우회 / 소원 사칭
   삽입. → 코드 수복 + 마이그레이션 2건, 전부 라이브 적용·검증 완료.
2. **기능 P0 2건** (A2, 프로덕션 실측 재현) — 오방기 전용 페이지 **영구 교착**(연출 CSS 미적재
   라우트에서 animationend 가 영원히 안 옴) / 액막이 진행 버튼이 가이드 바에 **차단**(z 역전).
   → 야간 수복·배포.
3. **디자인의 실체적 원인** (A4) — "형식적"의 물리 원인 중 하나가 **골드 위계 37곳의 무증상
   미렌더**(tailwind 슬롯 부재로 클래스 미생성)였다. → 소생 + 의식 독 통합 등 P1 집행.

프로덕션 보안 헤더 실측 정상(HSTS·CSP 등 6종) — 7/11부터 남아 있던 검증 항목 해소.

## 1. 감사 체계

| 트랙           | 질문                                               | 보고서                                          |
| -------------- | -------------------------------------------------- | ----------------------------------------------- |
| A1 기획 정합   | 기획대로 설계되고 있는가                           | docs/REPORTS/AUDIT-20260731-plan-conformance.md |
| A2 버그·디버깅 | 어디가 터질 수 있는가 (+프로덕션 e2e 첫 실행)      | docs/REPORTS/AUDIT-20260731-bugs.md             |
| A3 보안        | 악용 가능한 표면이 있는가 (+라이브 DB 대조)        | docs/REPORTS/AUDIT-20260731-security.md         |
| A4 디자인      | "형식적" 느낌을 어떻게 벗는가 (실행 가능 스펙으로) | docs/REPORTS/AUDIT-20260731-design.md           |

## 2. A1 기획 정합 — 결과 확정분

**총평: 기획대로 가고 있다.** 게임필 부록 A~E 확정 사항 거의 전량 이행(자유 팬·폭 320·입장 걸음
파생·9국면 회전·접지 계약·좌석 전구역·reduced-motion). 의식 3종은 프라이버시 약속(원문 무저장)이
스키마 강제 + 액션 시그니처 + 테스트의 3중으로 **기획보다 강하게** 들어갔고, 과금은 "무료 우선 →
명시 동의 → 실패 환불" 순서라 구조적으로 사고가 안 난다. 마이그레이션 6건 전부 라이브 적용 확인.

**기능 격차 P1 3건**
| # | 격차 | 판단 |
| --- | --- | --- |
| G1 | 합동 기도 **가족 기원 보너스(+1)** 미구현 — 만개 연출만 있음 (PRD 안3) | 재화 지급 로직 — 야간 자율 범위 밖(지급 정책은 CEO 확인 후) |
| G2 | 축원 영상 파이프라인 미착수(자리만) | 첫 완주자까지 ~100일 여유 — 별도 차수, 아침 결정 |
| G3 | 오방기 신당지기 연출이 전용 페이지 이관 때 기록 없이 소멸 | 페이지에서의 대체 연출을 A4 디자인안과 함께 판단 |

**의도적 표류(전부 코드 주석에 사유 있음 — 정당)**: wish_text 폐기(실물 굿 보류의 귀결) ·
완주 자동 확정 → 수동 갈무리(연출 없는 트로피 방지) · 트로피 3등급·백일 소원끈 신설(유익 방향).

**문서 부채 8건(P2)**: PRD 부록 F 부재(6차 검수 11건 — 세로선 진범=문틀 그림자 포함) ·
ARCH 모듈명 불일치 4건 · 로드맵 v2에 의식 트랙 부재 · MEMORY의 "마이그레이션 미적용" 낡은 기록 등.

## 3. A2 버그·디버깅 — 게이트는 초록인데 실브라우저에서 2개가 죽어 있었다

게이트 4/4 초록(tsc·jest 1,275·build·CSS 게이트) — 그러나 프로덕션 e2e(테스트 계정 로그인
실브라우저, 이 세션 최초 실행)에서 **콘솔 에러 0의 무증상 사망**이 실측 재현됐다.

| 심각도 | 결함                                                                                                                           | 원인                                                                                                                  | 수복                                                                                           |
| ------ | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| P0     | 오방기 전용 페이지 **영구 교착** — "방울이 울리고…"에서 정지. 직접 진입·새로고침·프로필 버튼 전부. 유료 경로 포함 전면 사망    | `shrine-scene.css` 가 방 페이지 체인에서만 적재 — 전용 라우트엔 키프레임이 통째로 없어 `animationend` 가 영원히 안 옴 | ✅ 신당 **layout** 에서 적재(개별 페이지 import 는 또 빠뜨린다 — 게이트와 같은 원리로 한 곳에) |
| P0     | 액막이 진행 버튼이 **가이드 바에 차단** — 할 일 ≥1 계정 전원 의식 불가                                                         | 시트 z-toast(60) < 가이드 바 z-[125] (코드 주석은 반대로 적혀 있었음)                                                 | ✅ 시트 z-[130]                                                                                |
| P1     | 연소 완료 후 **다 탄 부적이 원상복구**된 채 마무리 카드 위에 등장                                                              | settled 에서 burning 클래스가 내려가며 마스크가 벗겨짐                                                                | ✅ settled 에서 무대 미렌더                                                                    |
| P2×6   | 의식 클라이언트 실패 경로 부재(.catch 없음 → 고착) · reload 가 보상 토스트 즉시 소멸 · 환불 실패 무기록 · 진부 e2e 스펙 2본 등 | —                                                                                                                     | 잔여(§8)                                                                                       |

e2e: 공식 5스펙 중 3 pass / 2 fail(둘 다 스펙 진부 — 제품은 정상) / 1 skip. 커버리지 공백 발견:
테스트 계정이 `choga` 테마(반가 미보유)라 **창방 팻말·와이드룸이 e2e 사각지대**다.
서버 층은 견고 판정(과금 순서·멱등·환불·RLS — 의심 4건 검토 후 기각).

## 4. A3 보안 — 신규 11건 (P0 3 / P1 3 / P2 5) + 기존 잔여 4건

**오늘 배포분의 DB 층은 통과.** 신규 RPC 5종 전부 service_role 전용 + search_path 고정(어드바이저
신규 지적 0), 신규 테이블 3종 RLS 정상. 설계가 막겠다고 선언한 것(이중지급·과금 순서·원문 유출·
프로토타입 오염)은 전부 실제로 막혀 있음이 라이브 대조로 확인됐다.

**P0 — 오늘 밤 수복 (실제 악용 경로)**
| # | 결함 | 악용 | 수복 방향 |
| --- | --- | --- | --- |
| S-P0-1 | `shrines_update_own` 이 **컬럼 제한 없는 UPDATE** 를 열어 둠. 서버는 `owned` 를 계산하고도 안 씀 | `PATCH /rest/v1/shrines {main_deity_id, active_pack_id}` 한 방으로 유료 신위(39,900원)·유료 테마(19,900원) 무료 착용 | ①신위·테마 착용 액션을 소유권 검증 + admin 경유로 ②배포·회귀 ③컬럼 권한 축소(GRANT UPDATE 컬럼 화이트리스트) — 순서 고정(S1b 교훈) |
| S-P0-2 | 오방기·액막이 RPC가 **잠금 없는 count-then-insert** | 50 병렬 요청 → 무료 50회(회당 1만냥 회피) | RPC에 `pg_advisory_xact_lock` 1줄 (CREATE OR REPLACE) |
| S-P0-3 | `wishes_insert_any`(WITH CHECK true) — 오늘 배포한 좌석 presence가 `is_owner_wish` 를 소비하며 영향 확대 | 남의(비공개 포함) 신당에 anon 으로 소원 삽입·좌석 점등 조작 | WITH CHECK 강화(is_owner_wish 는 소유자만·비공개 신당 차단). 기존 방문 소원 기능은 보존 |

**P1 — 오늘 밤 수복 2건 + 문서화 1건**

- 의식 액션 4종 **멤버십 게이트 부재**(페이지는 layout 이 막지만 액션은 공개 엔드포인트 — 같은 커밋의
  family-hall.ts 는 걸어 뒀음) → 동일 게이트 이식.
- 의식 액션 **rate limit 부재**(상한 RPC는 거절 호출을 안 셈) → 기존 check_rate_limit 연결.
- 오방기 **시드 역산 = 실제 가능(YES)**. 단 색에 재화가 안 걸려 있어 경제 피해 0 —
  "색 기반 보상 추가 시 서버 판정 전환 필수"를 코드 주석 + 본 문서에 못박고 야간 리팩터는 하지 않음.

**기존 잔여**: R1·R2·R4·R5 라이브 해소 확인 / R7(`admin ?? supabase` 폴백)은 S1b 이후 위험도 상승
(조용한 실패) → 야간에 폴백 제거 / R6·R10 잔여는 Track S 로.

## 5. A4 디자인 품질 상향 — "형식적"의 물리적 원인 5가지 + 처방

**핵심 발견: 골드 위계가 실제로 죽어 있었다.** `text-gold-200`(신당 37곳 — 스트립 이름표·의식
시트 제목·CTA·가이드바)이 tailwind 설정에 **없는 슬롯**이라 클래스가 아예 생성되지 않았다
(빌드 청크 grep 0건 실측, 대조군 gold-300 은 정상). 골드 라벨이 전부 흰 상속색으로 렌더되고
있었다 — CEO가 반복해서 느낀 "형식적"의 실체적 원인 중 하나. styled-jsx 사건과 같은
**무증상 사망** 계열이다.

| #    | 원인                                       | 처방                                                                 |
| ---- | ------------------------------------------ | -------------------------------------------------------------------- |
| P1-0 | 죽은 클래스(gold-200·gold-dim·white/8)     | 슬롯 등록 + 치환 + **빌드 CSS grep 게이트** — ✅ 야간 집행 완료      |
| P1-1 | 동형 스트립 5곳 반복(목록처럼 읽힘)        | **의식 독(儀式 dock)** — 카드 하나 + 신당 최초의 단청 리듬 + 44px 행 |
| P1-2 | 도장 CTA 규약(3px) 실종 — 신기능 전부 12px | 버튼 반경 3단 문법(주=도장 3px / 보조=8px / 칩=full)                 |
| P1-3 | 팻말 각자 품질                             | 금박 그라디언트 + 홈 그림자 + 900 (실물 널 렌더로 B안 검증)          |
| P1-4 | 모아보기 이중 헤더·CTA 문법 혼재           | 헤더 단일화 + 문법 통일                                              |
| P1-5 | 하단 네비 가운데 슬롯 무강조               | 신당 슬롯 상시 앵커화(온건판)                                        |

P2 8건(타이포 정규화·저투명 승급 등)과 에셋 필요 3건은 **아침 승인 대상**으로 분리.
불가침: 연소 연출·오방기 실물 형태·신위 회전·무라 좌표계.

## 5-b. 야간 실행 현황 (실시간)

- ✅ **보안 코드 수복 4파일**: 테마 착용 admin 전환(S-P0-1 코드측) · 의식 4종 멤버십+rate limit
  게이트(ritualGate) · R7 폴백 제거(attendance·daily-check 6개소, 조용한 강등 → 시끄러운 실패)
- ✅ **마이그레이션 1/2 라이브 적용**: RPC 어드바이저리 잠금(S-P0-2) + 소원 정책 교체(S-P0-3 —
  사칭·비공개 신당 삽입·주인 표식 위조 차단, 정당 흐름 3종 보존)
- ✅ **P1-0 집행**: gold-200 슬롯 등록·gold-dim→gold-600·white/8 수정 — 빌드 산출물 생성 확인
- ✅ A2 P0 2건 + P1 1건 수복(위 §3)
- ✅ 디자인 P1-1 의식 독(RitualDock) · P1-2 도장 CTA(대부분) · P1-4 모아보기 — 집행 완료
  (실행 에이전트가 세션 한도로 중단됐으나 산출물은 게이트 통과 상태로 회수)
- ⏸ **P1-3 팻말 각자 · P1-5 네비 앵커 — 미착수 잔여**(오전 이어서, 코드만)
- ⏳ 마이그레이션 2/2(shrines 컬럼 권한 축소)는 코드 배포 직후 적용(S1b 순서 규율)

## 6. 야간 실행 원칙 (자율 범위의 경계)

1. **AI 이미지 생성 금지** — 17신위 확산(≈$2)·G-4 테마 확산은 "시범 검수 후 양산" 게이트 유지.
   앰비언트 5회 반려의 교훈. 아침 GO 대기.
2. **재화·지급 정책 신설 금지** — 합동 기도 보너스 같은 지급 로직은 금액·정책이 기획 결정.
3. **마이그레이션은 추가 전용만**, 적용 전 라이브 스키마 대조.
4. **CEO 검수 통과 요소 불가침** — 연소 연출·오방기 실물 형태·신위 회전·무라 퀼팅.
5. 배포는 통합 게이트(tsc·jest·build·check-animation-css) 전량 통과 후.

## 7. CEO 결정 대기 큐 (아침에 답 주시면 됩니다)

| #    | 결정                                                                | 맥락                                                        |
| ---- | ------------------------------------------------------------------- | ----------------------------------------------------------- |
| N-1  | **17신위 턴 프레임 확산 GO** (≈$2)                                  | 시범 2신위(선녀·용왕)는 6차까지 통과 상태                   |
| N-2  | **G-4 테마 7종 확산 GO**                                            | 동결 사유(스타일 미확정)는 사실상 해소 — 퀼팅·2단 제단 확정 |
| N-3  | 오방기 **노랑 = "무난" vs 전승 "조상"**                             | 전승 조사 결과와 구현이 다름. 판정 로직은 미변경            |
| N-4  | 가족 좌석 드래그 **가장자리 자동 팬**                               | 지금은 끌기→팬→끌기 반복. 편의 vs 오조작 위험               |
| N-5  | **축원 영상 파이프라인 착수 시점**                                  | 자리(상태·큐·RPC)는 완성. 렌더·Storage·서명 URL이 별도 차수 |
| 이월 | D-5(GitHub 시크릿·Sentry rule) · D-6(카카오 JS 키) · D-8(.env 구키) | 콘솔·자격증명 — 에이전트 불가                               |

## 8. 실행 결과 & 잔여

**야간 집행 완료(배포분)**: 보안 P0×3·P1×2(게이트·rate limit)·R7 / 기능 P0×2·P1×1 /
디자인 P1-0·1·2·4 / 문서(본 기획서 + 감사 보고서 4건).

**잔여 — 코드만으로 가능(승인 불요, 오전 이어서)**
| # | 항목 | 출처 |
| --- | --- | --- |
| R-a | 디자인 P1-3 팻말 각자 · P1-5 네비 앵커 | A4 |
| R-b | 의식 클라이언트 실패 경로(.catch·finally) · reload→router.refresh 전환 검토 · 환불 실패 로그 | A2 P2 |
| R-c | 진부 e2e 스펙 2본 갱신 + 테스트 계정에 반가 테마 부여(팻말 e2e 사각 해소) | A2 |
| R-d | 문서 부채 8건(PRD 부록 F·ARCH 모듈명·로드맵 의식 트랙) | A1 |

**아침 결정 필요 — §7 결정 큐(N-1~N-5)와 A4 P2·에셋 3건.**

## 9. 아침 보고

`docs/REPORTS/REPORT-20260731-overnight.md` 에 실행 결과·배포 여부·잔여 항목 정리.

---

## 부록 P. 배치 자유도 v2 — 「층은 문법, 위치는 자유」 (CEO 7차 지시)

지시: "신당 디자인과 배치UI 다시 확인하고 수정기획해줘 … 자유도높은신당으로 만들어줘,
아직도 배치가 좀어색해" (2026-07-31). 수정기획 먼저 — 진단은 합성 렌더·라이브 DB 실측 기반.

### P-1. 진단 — "어색함"의 물리 원인 4가지 (증거 기반)

합성 렌더(와이드 룸 1664×620 + 현행 존 오버레이):
`D:/anti/media/shrine-zone-diagnosis/zones-current.png` (전폭) · `zones-current-viewport.png` (정지 카메라 화면)

| #   | 원인                                                                                                                                                                                                                      | 증거                                                                                                                                                            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-1 | **죽은 띠 3개가 하필 가구 위에 있다.** 존 4층(8~20/23~42/48~56/64~90) 사이 공백 y 20~23·42~48·56~64 는 배치 불가인데, 42~48 은 **단상 상면**(y 42.4~48), 56~64 는 **제단 상판 앞쪽 절반**(상판 49.4~66.6)과 정확히 겹친다 | 합성 렌더에서 육안 확정 — 빗금 띠가 단상 위·상판 앞턱을 덮는다. 드래그하면 가구 한가운데의 보이지 않는 선에서 클램프로 튕긴다                                   |
| D-2 | **altar 존 x[24,76]가 와이드 룸 이전 값이다.** 배치 x 는 대청 로컬 %(폭 320 방 전체)로 재해석됐는데 존은 안 따라왔다 — 보이는 상판은 로컬 x 40.9~59.1(0.58화면), 존은 24~76(2.6화면). 존의 68%가 "상 높이의 허공"         | **라이브 실측: altar 배치 14건 중 6건(43%)이 상 밖(x<38 또는 x>62)** — 공중에 뜬 공물. min/max 가 정확히 24/76 = 클램프 벽에 밀어붙인 흔적                      |
| D-3 | **앵커 좌·우(x34/66)가 상판 밖 허공이다.** 안2.3 시드가 y 만 상판 면(53.5)으로 옮기고 x 는 좁은 방 값(34/66)을 승계 — 와이드 룸에서 상판은 40.9~59.1 이라 「제단 왼편/오른편」 자석이 **허공으로 끌어당긴다**             | 합성 렌더에서 링 2개가 상 밖에 떠 있음. 스냅 반경 6도 로컬 단위라 실제 포획 타원이 **가로 100px×세로 37px**(의도의 3.2배) — 상판 위 자유 배치 폭의 75%가 자석권 |
| D-4 | **가족 좌석과의 비대칭.** 좌석은 HALL_SEAT_ZONE(사실상 방 전 구역, y 11~94)인데 아이템만 띠에 갇혀 있다. hanging 배치 실측 min/max x 가 정확히 8/92 — 사용자가 벽에 막힌 흔적                                             | family-hall-layout.ts 대비 zones.ts. 라이브 hanging 7건 x 8.00~92.00                                                                                            |

결론: "어색함"은 사용자의 배치 실력 문제가 아니라 **시스템이 만든 것**이다 — 가구 위의 보이지
않는 벽(D-1), 세대교체를 안 따라온 존(D-2), 허공으로 끌어당기는 자석(D-3), 좌석만 자유(D-4).

### P-2. 기획 — 무엇을 풀고 무엇을 남기나

원칙: **층(placement_layer)은 z-순서·그림자·원근·크기의 문법으로만 남기고, 위치 제약은
물리적 최소만 남긴다. 겹침은 허용한다(painter's algorithm 이 이미 처리한다).**

**새 존 (v1 → v2, 전 축 확장 — 좁아지는 축 0):**

| 층      | v1                | v2                | 남긴 물리 제약(왜)                                                                                             |
| ------- | ----------------- | ----------------- | -------------------------------------------------------------------------------------------------------------- |
| hanging | x[8,92] y[8,20]   | x[4,96] y[4,42]   | **벽 상부만** — 매달릴 곳(창방·벽)이 있어야 한다. 하한 42 = 단상 상면(42.4) 직전. 마루엔 매달 수 없다          |
| wall    | x[8,92] y[23,42]  | x[4,96] y[4,60]   | **벽면만** — 마루선(62) 위까지. 창방(y4~10)에 팻말을 거는 것도 실물 문법이라 상단도 연다                       |
| altar   | x[24,76] y[48,56] | x[24,76] y[48,58] | **하한 48 불가침**(안2.5 — 상판 뒤 허공에 놓이면 상에 가려진다, 라이브 3건 실측 이력). 상한 +2 = 상판 앞전까지 |
| floor   | x[8,92] y[64,90]  | x[4,96] y[44,96]  | **마루+단상 위** — 상한 44 = 단상 상면부터(신위 곁에 세울 수 있다). 방 밖 금지(여백 4)는 전 층 공통            |

죽은 띠 소멸: y 4→96 이 hanging∪wall∪floor 로 빈틈없이 덮인다(겹침 구간은 층 문법이 정렬).
제안 렌더: `D:/anti/media/shrine-zone-diagnosis/zones-v2.png` · `zones-v2-viewport.png`

**altar x[24,76]를 좁히지 않는 이유(D-2 인데 왜):** ①레거시(단일 무대) 테마의 제단은 뷰포트
x 19~81 을 실제로 덮는다 — 좁히면 레거시가 죽는다. ②좁히면 기존 배치 6건이 저장 시 이동한다
(무손실 계약 위반). ③"시스템이 허공에 놓는" 경로는 앵커 이설(P-3)과 스냅 개선으로 끊고, 사용자가
스스로 상 밖에 놓는 것은 자유의 영역이다. 테마별 상판 실폭 클램프(무대 파생 존)는 서버 저장
경로에 테마 로드가 필요해 복잡도 대비 이득이 없어 **기각**(후속 재론 가능 옵션으로만 남긴다).

**원근(depthScale)·z·그림자 — y 자유화와의 충돌 차단:**

- floor 원근 램프의 기준 밴드를 존에서 **분리·동결**한다: `FLOOR_DEPTH_REF = [64, 90]`
  (마루 개활지 실측 기준). 존을 [44,96]으로 넓혀도 기존 아이템 크기·그림자·z 가 **비트 단위로
  동일**하다(전 라이브 floor 배치가 64~90 안). 밴드 밖은 경계 클램프: 단상 위(y<64)는 최소
  스케일 0.72(멀다=작다 ✓), 앞전(y>90)은 1.28 유지.
- 깊이 정렬 1분기 추가: floor 이면서 **y < 48(제단 존 하한 = 상판 뒤)** 는 z 12 — 제단(14)보다
  뒤에 그린다. 단상 위 소품이 공물상을 덮는 역전 방지. y≥48 은 기존 16~28 램프 그대로(기존
  배치 z 불변).
- altar 0.88 고정·wall/hanging 1.0 은 y 무관이라 충돌 없음.

**자석 스냅 — 반경 축소 + 시각 거리계 교정 (D-3):**

- 거리를 로컬 % 가 아니라 **화면 px** 로 잰다: `nearestAnchor` 에 축 가중치(scale) 인자를 추가하고
  드래그 핸들러가 방 실측(rect)을 넘긴다. 반경 34px(=앵커 링 지름) — 좁은 레거시 방에서는 종전
  6% 와 거의 같고(≈31~37px), 와이드 룸에서는 가로 포획이 100px→34px 로 준다. "천천히 끌면
  자유" 같은 속도 휴리스틱은 **기각** — 진단상 문제는 스냅의 존재가 아니라 포획 폭의 3.2배
  왜곡이었다. 거리계를 고치면 앵커는 다시 "보너스"가 된다.
- 초기 배치 기준선 분리: `initialSpot` 의 y 를 존 중점 파생에서 **동결 상수(HOME_Y)** 로 —
  hanging 14·wall 32.5·altar 52·floor 77(전부 오늘의 중점값). 존이 넓어져도 새 아이템이
  떨어지는 자리는 지금과 동일하다.
- 레거시 폴백 앵커(DEFAULT_ANCHORS)도 존 파생에서 **동결 리터럴**로 — 존 확장이 레거시 테마의
  "의미 있는 자리"를 밀어내지 않는다(ALTAR_ANCHOR_Y 동결과 같은 원리, 좌표 전과 동일).

**존 가이드 오버레이 — 상시 4띠 우리(cage) → 드래그 중인 층 1면:**
꾸미기 진입 시 아무것도 안 그리고, 아이템을 잡는 순간 그 층의 허용면 하나만 하이라이트한다.
새 존은 서로 크게 겹쳐 4면 동시 표시는 소음이고, "우리에 갇힌" 인상 자체가 어색함의 일부였다.

**시드 데이터 정정(D-3):** 반가 와이드 상판 앵커 x 34/66 → **45/55** (y 53.5 유지, 상판
40.9~59.1 안). 마이그레이션 파일 `20260731_shrine_banga_altar_anchors_on_table.sql` 작성 —
**적용은 하지 않는다**(본 트랙 금지 사항). 코드 의존이 없어 코드 배포 전후 어느 시점 적용도
안전하다. 라이브에 좌·우 앵커로 스냅된 배치 0건(실측)이라 데이터 이관 불요.

### P-3. 무손실 계약 (기존 배치 영향 = 0)

- 전 축 **확장만** — v1 범위 ⊆ v2 범위를 테스트로 강제한다(zones.test.ts). 좁아지는 축 없음.
- 라이브 배치 29건(altar 14·floor 8·hanging 7, 신당 4곳) 전수: v1 클램프 안 = v2 안. 이동 0.
- 원근·z·그림자: 기준 밴드 동결로 기존 y 값에서 산출이 전부 동일(위 P-2).
- 서버 클램프는 저장 시에만·같은 ZONES 단일 출처(클라=서버) 유지. altar 하한 48 유지.

### P-4. 게이트·검증

tsc · jest(zones/stage/banga-wide-seed 계약 갱신 + zones.test 신설) · build ·
check-animation-css(신규 CSS 0 — 가이드는 인라인 스타일). 렌더 검증은 위 합성 이미지 2쌍.
