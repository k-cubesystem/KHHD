# PRD-shrine-gamefeel: 신당 게임필(Game-Feel) 전면 개선 — 다각 개선안

버전: v1.0 | 작성: TEAM_G (PRD_MASTER) | 날짜: 2026-07-29
상태: DRAFT — **사용자 컨셉 선택 대기** (안별 프로토타입 → 하나씩 검수 → 확정 후 확산)

---

## 1. 개요

### 문제 정의 (CEO 원문 진단)

> "컨셉이나 기획은 너무 좋은데 이미지 디자인·UI가 형식적이다. **실제 게임하는 듯한
> 느낌의 액션과 효과**가 필요하다. 지금은 **배경이미지에 상만 얹혀놓은 느낌** —
> 이대로는 소비자에게 무시당한다. 가로 스크롤 넓은 방도 좋고, 가족이 함께 모이는
> 방 컨셉도 좋다. 3D나 픽셀도 좋으니 개선안을 여러 개 기획하라."

v2 「조립식 무대」로 무대 물리(원근 스케일·접지 그림자·조명 그레이딩·앵커)까지
넣었지만, 여전히 "정지된 디오라마"로 읽히는 이유를 게임 문법으로 해부하면:

| #   | 게임을 게임처럼 느끼게 하는 요소     | 현행 신당 상태                                                                  |
| --- | ------------------------------------ | ------------------------------------------------------------------------------- |
| 1   | **카메라** — 화면보다 큰 공간을 탐색 | 없음. 방 1장 = 화면 1장, 시점 고정 (`min(56vh,480px)` 단일 프레임)              |
| 2   | **시간** — 가만히 있어도 세계가 산다 | 거의 없음. 유일한 상시 모션 = 파티클·AmbientVideo. 아이템·배경·신위는 완전 정지 |
| 3   | **주민** — 살아있는 캐릭터           | 신위 = 정지 초상 1장 + 말풍선. 걷기·호흡·시선 없음. 가족은 존재 자체가 없음     |
| 4   | **주스(juice)** — 타격감·과장 반응   | 일부 있음(탭 사운드·파티클·바운스). 화면 흔들림·스쿼시·햅틱·연출 카메라 없음    |
| 5   | **의식(ritual) 연출** — 빅 모먼트    | 없음. 기도·봉헌·해금이 전부 "버튼 → 토스트 1줄"로 소비됨                        |

**결론: 에셋 품질 문제가 아니라 "정지 화면" 문제.** 5요소 중 4개가 비어 있어
어떤 그림을 얹어도 "배경 위에 스티커"로 읽힌다. 개선안은 이 5요소를 채우는
순서·방식의 조합이다.

### 목표

- 신당을 "확인하는 화면"에서 **"머무는 장소"**로 — 게임 수준의 공간감·생동감·타격감
- 체류·재방문 상승 → 기도(기원)·꾸미기 루프 강화 → 테마/신물 구매·멤버십 전환 견인

### 성공 지표 (KPI)

| 지표                               | 현행 대비 목표 | 측정                          |
| ---------------------------------- | -------------- | ----------------------------- |
| 신당 평균 체류시간                 | +50%           | GA4 `page_view`→이탈 간격     |
| 신당 일 재방문율(DAU 중 신당 진입) | +20%p          | GA4 `shrine_enter`            |
| 탭 인터랙션 수/세션                | 2배            | `shrine_tap`·`keeper_give` 등 |
| 기도(기원) 완료율                  | +15%p          | `devotion_pray`               |
| 테마·신물 구매 전환                | +30%           | `pack_activate`·구매 이벤트   |
| FAMILY 멤버십 전환(안3 배포 시)    | 신규 퍼널      | 사랑방 진입→업셀 클릭→결제    |

---

## 2. 유저 스토리

| ID  | As a...           | I want to...                                       | So that...                            | 우선순위 |
| --- | ----------------- | -------------------------------------------------- | ------------------------------------- | -------- |
| U1  | 신당 소유자       | 방에 들어설 때 문이 열리고 불이 켜지는 연출을 본다 | 내 신당이 "장소"로 느껴진다           | P0       |
| U2  | 신당 소유자       | 가만히 봐도 촛불이 흔들리고 향이 피어오른다        | 살아있는 공간이라 오래 머문다         | P0       |
| U3  | 신당 소유자       | 기도가 조명·카메라가 움직이는 의식으로 연출된다    | 매일 기도할 맛이 난다(기원 리텐션)    | P0       |
| U4  | 신당 소유자       | 방을 옆으로 넘기며 마당·대청·후원을 오간다         | 공간이 넓어 더 꾸미고 싶다(구매 동기) | P1       |
| U5  | 가족 관리자(4050) | 가족들이 사랑방에 모여 앉은 모습을 본다            | 가족 사주 관리의 정서 보상이 생긴다   | P1       |
| U6  | 가족 관리자       | 오늘 기도 안 한 가족의 빈 방석을 보고 챙긴다       | 가족 전원 기도 루프가 돈다            | P1       |
| U7  | 20-30 커플/수집가 | 도트(픽셀) 감성 신당 테마로 갈아입힌다             | 취향 표현 + 신선함(유료 테마 구매)    | P2       |
| U8  | 저사양 폰 사용자  | 연출이 무거우면 자동으로 가벼운 모드가 된다        | 어떤 폰에서도 쾌적하다                | P0(NFR)  |

---

## 3. 개선안 5종 — 각각 독립 프로토타입·검수 가능

> 배타 선택이 아니다. **안1~3은 같은 무대 위에 쌓이는 레이어**(카메라·시간 → 공간 →
> 주민)이고, **안4~5는 아트 문법 선택지**다. 권장 조합은 §4.

### 안1 「살아 숨쉬는 신당」 — 주스·시간·의식 패스 (에셋 0, 코드만)

- **한 줄**: 지금 방 그대로, "정지 화면"을 죽이는 모든 것 — idle 모션·낮밤 조명·
  입장/기도 시네마틱·타격감.
- **연출 상세**
  - **상시 idle**: 촛불 스프라이트 흔들림+광원 플리커, 향 연기 상시 피어오름, 족자·
    풍경(風磬) 미세 살랑, 신위 호흡(scale 1±0.5%, 4초 주기)·눈 깜빡임 대체로 글로우 맥동
  - **낮밤 사이클**: KST 시간대별 광원 프리셋(새벽 청람 → 낮 백금 → 석양 주홍 → 밤
    촛불금). 기존 `StageLight` 그레이딩 값만 시간 보간 — 방이 하루를 산다
  - **입장 시네마틱(1.2s)**: 암전 → 문살 그림자 스치며 카메라 살짝 푸시인 → 광원
    점등 → 신위 인사. 재방문은 축약(0.5s), `prefers-reduced-motion` 시 생략
  - **기도 의식(3s)**: 기도 버튼 → 주변 UI 페이드아웃 → 카메라 제단 푸시인 → 촛불
    일제 점화 → 신위 응답 대사 → 기원 +1 카운트업 + 종소리. 매일 보는 "빅 모먼트"
  - **주스 강화**: 탭 스쿼시&스트레치(0.9→1.05→1), 빅 모먼트 미세 화면 흔들림(2px),
    `navigator.vibrate(10)` 햅틱, 사운드 레이어링(기존 useShrineAudio 재사용)
- **게임 레퍼런스**: 모뉴먼트 밸리 입장 연출, 젤다 사당 점등, 방치형 게임 idle 문법
- **재사용**: EffectsCanvas·useShrineAudio·StageLight·keeper-lines 전부 그대로 확장
- **공수**: CC 1~2세션 | **리스크**: 최소(에셋 0, 렌더 스택 유지)
- **수익 연결**: 기도 리텐션(기원 단 해금 루프) 직접 강화
- **검수 포인트**: ①입장 연출 톤 ②밤/낮 광원 색 ③기도 의식 길이(지루하지 않은가)

### 안2 「두루마리 신당」 — 가로 스크롤 확장 (CEO 아이디어 ①)

- **한 줄**: 방을 가로 2.4화면 폭 두루마리(橫卷)로 펼치고, 손가락으로 카메라를
  팬(pan)하며 **마당 — 대청 — 후원** 세 구역을 오간다.
- **공간 설계** (논리 폭 240%, 뷰포트 100%)
  - **마당(앞뜰)**: 대문·석등·삽살개·장독대. 방문객이 처음 서는 곳 — 입장 연출 시작점
  - **대청(중앙)**: 현행 신당 그대로(제단·신위·배치). **기존 배치 좌표 무손실 이관**
  - **후원(뒤뜰)**: 곳간(수납·기억함)·가족 사당 자리(안3 예약 슬롯)·성장 시각화(기원
    단이 오를수록 후원이 풍성해짐)
  - 시차(parallax) 3층: 원경(하늘·담장 0.3배) / 무대(1배) / 전경 소품(1.15배) —
    팬 할 때 깊이가 "보인다"
  - 미니맵 점 3개 + 구역 자석 스냅. 두루마리 펼침 입장 연출(가로로 스르륵)
- **게임 레퍼런스**: 싸이월드 미니홈피 확장 판타지, 어몽어스 대기실, 원신 두루마리 UI,
  스타듀밸리 농장 팬
- **재사용**: StageLayers 구조(벽지/바닥/구조물) 그대로 — 구역별 세트로 3벌 조립
- **신규 필요**: 와이드 벽지·바닥(또는 타일), 마당·후원 구조물 스프라이트 6~10점
  (기존 `scripts/shrine-assets` 파이프라인 재사용)
- **공수**: CC 2~3세션 + 에셋 1세션 | **리스크**: 가로 팬 ↔ 세로 페이지 스크롤 ↔
  편집 드래그 제스처 충돌(ARCH §4에서 해소), 배치 공간 3배에 따른 아이템 희소감 저하
  (→ 구역별 배치 상한으로 방어)
- **수익 연결**: 배치 슬롯 3배 = 신물·구조물 판매 지면 3배. 구역 자체(후원 개방)를
  기원 단·멤버십 보상으로
- **검수 포인트**: ①팬 조작감(4050 여성 타깃 — 한 손 엄지) ②구역 3개의 첫인상
  ③기존 배치가 대청에 그대로 있는가

### 안3 「가족 사랑방」 — 가족이 모이는 방 (CEO 아이디어 ②)

- **한 줄**: 가족 멤버들이 **아바타로 실제로 방에 앉아 있는** 공간. 기도한 가족은
  촛불이 켜진 방석에, 안 한 가족은 빈 방석 — 가족의 오늘이 한 화면에 보인다.
- **연출 상세**
  - 가족별 방석+등불 배치(중앙 공동 제단 둘레). **기존 가족 아바타 5종
    (`lib/domain/family/avatars.ts`) 재사용** — 앉은 자세 스프라이트 5종만 추가 생성
  - **라이브니스**: 오늘 기도한 가족 = 아바타 착석+등불 점등, 미기도 = 빈 방석+꺼진
    등불(U6 "챙기기" 트리거), 신당 열람 중인 가족 = 아바타 글로우
  - **입장 연출**: 가족이 접속하면 문 열리고 걸어 들어와 방석에 앉는다(다른 가족
    화면에 실시간 아님 — 다음 진입 시 재생, 서버 폴링 없이 저비용)
  - **상호작용**: 아바타 탭 → 그 가족 오늘의 한 줄 운세/안부 말풍선. **합동 기도**:
    당일 전원 기도 달성 시 사랑방 만개 연출 + 가족 기원 보너스(+1 추가)
  - 배치 위치: 단독 화면(신당 탭 분기) 또는 안2의 후원 구역(권장 — §4)
- **게임 레퍼런스**: 동물의 숲 광장, 스타듀밸리 축제, 카카오프렌즈 타운
- **재사용**: family_members·가족별 신당·devotion RPC(기도 여부)·아바타 5종 —
  데이터는 전부 있음, **시각화만 없던 상태**
- **공수**: CC 2세션 + 착석 스프라이트 5종 | **리스크**: 가족 없는 SINGLE 사용자에게
  빈 화면(→ SINGLE은 "가족 초대" 업셀 씬으로 전환), 가족 이름 노출 프라이버시(기존
  공개 확인 플로우 연장)
- **수익 연결**: **FAMILY 멤버십(30만/일)의 킬러 화면.** 사랑방 잠금 해제 = FAMILY
  가입 동기. 가족 방석·등불 스킨 = 신규 판매 품목
- **검수 포인트**: ①아바타 착석 모습 톤 ②빈 방석의 정서(죄책감이 아니라 애틋함으로)
  ③SINGLE 사용자 업셀 씬

### 안4 「도트 신당」 — 픽셀 아트 테마 라인 (CEO 아이디어 ③-a)

- **한 줄**: 스타듀밸리·모동숲 감성의 16-bit 도트 신당 — **전면 전환이 아니라
  유료 테마 라인**으로 판다.
- **연출 상세**: 방·제단·신물·신위 전부 도트 + **스프라이트시트 프레임 애니**(촛불
  4프레임, 향 연기 8프레임, 신위 2프레임 호흡, 삽살개 꼬리). 도트는 프레임 애니
  제작이 값싸서 "전부 움직이는 방"을 최저 비용으로 달성 — 게임 문법이 즉각 전달됨
- **브랜드 정합 (중요)**: DESIGN.md 포지셔닝은 "조선 반가 럭셔리". 도트 **전면
  전환은 브랜드 충돌** — 「설빛온기」 본선과 병행하는 **컬렉터블 테마 팩**(복고
  한정판, 기원 고단 해금 or 프리미엄 가격)으로 격리. 실패해도 본선 무손상
- **재사용**: 렌더 스택 그대로(`image-rendering: pixelated` + 스프라이트시트 CSS
  스텝 애니) — 별도 엔진 불필요
- **신규 필요**: 도트 에셋 풀셋(방 1 + 품목 12 + 신위 1 시범). 파이프라인: gemini
  생성 → 픽셀화 후처리(감색+다운스케일) 스크립트 신설
- **공수**: 파이프라인 1세션 + 에셋 1~2세션 + 렌더 0.5세션 | **리스크**: 에셋 톤
  반려 전례(앰비언트 v1~v5) — **반가 도트 1종 시범 검수 필수**
- **수익 연결**: 신규 유료 테마 라인(시즌 한정판 반복 출시 축)
- **검수 포인트**: ①도트 해상도 감(너무 거칠지/곱지 않은지) ②프리미엄 톤과의 공존

### 안5 「입체 신당」 — 3D (CEO 아이디어 ③-b)

- **한 줄**: low-poly 한옥 방을 실제 3D로 — 제한 오르빗(±15°)·실시간 촛불 광원·
  그림자. 차별화 최대, 공수·리스크도 최대.
- **기술**: react-three-fiber + drei. 번들 +150KB↑, 중저가 안드로이드 GPU 부담,
  기존 2D 배치 좌표계와 비호환(3D 재매핑 필요), 에셋 전면 재제작(모델링 파이프라인
  신설 — 이미지 생성으로 해결 불가)
- **판단**: 안1~3으로 "게임 느낌"의 8할을 훨씬 싸게 달성 가능. 3D는 그 후에도
  차별화가 더 필요할 때의 **시즌2 카드**. 이번 라운드는 **보류 권고** — 단,
  2.5D 착시(안1 카메라 푸시인+시차)로 "입체감"은 선취한다
- **공수**: CC 5세션+ | **검수 포인트**: (진행 시) 프로토 1씬 성능 실측 선행

---

## 4. 우선순위 & 권장 테스트 순서 (하나씩 검수)

Impact(게임필 체감+수익) × Effort:

| 안  | Impact | Effort | 판정                           |
| --- | ------ | ------ | ------------------------------ |
| 안1 | ★★★★   | ★      | **즉시 — 1차 테스트**          |
| 안2 | ★★★★   | ★★★    | **2차 — 공간 골격**            |
| 안3 | ★★★★★  | ★★★    | **3차 — 킬러 씬(FAMILY 업셀)** |
| 안4 | ★★★    | ★★★    | 병행 가능 — 테마 라인 실험     |
| 안5 | ★★★★   | ★★★★★  | 보류(시즌2)                    |

```
1차  안1 살아 숨쉬는 신당   ← 에셋 0·코드만. "게임 같다"의 최소 증명. 즉시 착수 가능
2차  안2 두루마리 신당      ← 카메라가 생긴 방에 공간을 붙인다 (마당·대청·후원)
3차  안3 가족 사랑방        ← 후원 구역에 가족을 들인다 (FAMILY 킬러 씬)
병행 안4 도트 신당          ← 아트 실험은 본선과 독립 — 반가 도트 1종 시범만 먼저
보류 안5 3D                ← 안1~3 검수 후 재논의
```

각 차수 종료마다: **프로토타입 → 프로덕션 빌드 실기기 검수(사용자) → GO/NO-GO →
다음 차수**. 통과 못 하면 그 안만 폐기 — 앞 차수 결과물은 독립적으로 유효.

### ⚠️ v2 잔여 작업(테마 7종·11품목 스프라이트 확산)은 **동결**

방향 확정 전에 현행 단일 화면 규격으로 에셋을 확산하면, 안2(와이드 벽지)·안4(도트)
채택 시 **전량 재작업**이 된다. 확산은 이번 컨셉 확정 **후** 확정 규격으로 재개한다
(앰비언트 영상 5회 반려로 배운 것: 에셋은 방향 검수 후에만 양산).

---

## 5. 비기능 요구사항 (NFR)

| 항목        | 요구 수준                                                       | 측정                     |
| ----------- | --------------------------------------------------------------- | ------------------------ |
| 프레임      | 중급 안드로이드(Galaxy A 급)에서 팬·연출 중 55fps↑              | Chrome DevTools 실기기   |
| 입장 지연   | 시네마틱이 LCP를 막지 않음 — 연출은 로드 후 장식, 스킵 탭 허용  | Lighthouse/Sentry        |
| 접근성      | `prefers-reduced-motion` 전 연출 생략 경로, 시네마틱 스킵 탭    | e2e                      |
| 저사양 폴백 | `deviceMemory<4` 또는 프레임 드랍 감지 시 파티클·시차 자동 감축 | 코드 게이트              |
| 회귀 0      | 레거시 테마·기존 배치·촛불 저장 등 v2 계약 전부 유지            | jest + e2e + 수동 회귀   |
| 검증 환경   | **프로덕션 빌드로만 시각 검증**(dev CSS 불일치·rAF 사망 전례)   | launch.json `prod`(3001) |

## 6. 완료 기준 (DoD) — 차수별

- [ ] 1차(안1): 입장·기도 시네마틱 + idle 3종 + 낮밤 프리셋이 실기기에서 55fps, reduced-motion 경로 동작, 기존 배치·저장 회귀 0
- [ ] 2차(안2): 3구역 팬 + 시차 + 기존 배치 대청 무손실 + 편집 드래그와 제스처 무충돌
- [ ] 3차(안3): 가족 presence(기도 여부) 반영 착석 렌더 + 합동 기도 연출 + SINGLE 업셀 씬 + 가족 이름 노출 동의 플로우
- [ ] 4차(안4, 진행 시): 반가 도트 1종 시범 검수 통과 후에만 품목 확산
- [ ] 공통: tsc·eslint·jest·build 통과, GA4 신규 이벤트(`shrine_cinematic`, `shrine_pan`, `family_hall_view` 등) 계측, Sentry 무신규 에러

## 7. 제외 범위 (Out of Scope)

- 실시간 멀티(가족 동시 접속 라이브 이동) — presence는 "다음 진입 시 재생"으로 충분
- 안5 3D 구현(보류 결정 시), BGM 신곡 제작, 신당 외 페이지의 게임필 확장

## 8. 의존성 & 리스크

| 리스크                                    | 대응                                                                 |
| ----------------------------------------- | -------------------------------------------------------------------- |
| 에셋 톤 반려 반복(앰비언트 v1~v5 전례)    | 모든 신규 에셋은 1종 시범 → 사용자 검수 → 확산 (본 PRD 전 차수 공통) |
| 제스처 충돌(팬·스크롤·드래그)             | ARCH §4 각도 기반 제스처 라우터 + 편집 모드 분리                     |
| 고DPR 흰화면 전례(GPU 마스크·캔버스 폭주) | 카메라는 transform만 사용, 신규 캔버스 금지, DPR 클램프 유지         |
| 저사양 성능                               | NFR 폴백 게이트 + 프로덕션 실기기 검증 게이트                        |
| 가족 이름·아바타 노출                     | 기존 공개 확인 confirm 플로우를 사랑방에 연장                        |

---

_다음 문서: ARCH-shrine-gamefeel-v1.md (렌더 스택 결정·카메라 시스템·데이터 모델·제스처 설계)_

---

## 부록 A. 안2.1 「큰 방 하나」 — 2026-07-29 밤 CEO 실기기 검수 피드백 반영 (v1.1)

CEO 검수 결과(스크린샷 3장): 구역 3개 방식은 **방 두 장을 이어붙인 슬라이드**로 읽힘
(대청↔마당 경계 절벽, 실내↔실외 문법 단절). 원하는 스타일 아님 — 재설계 확정.

| 항목      | v1(폐기)                     | **v2.1(확정)**                                                              |
| --------- | ---------------------------- | --------------------------------------------------------------------------- |
| 공간      | 마당·대청·후원 구역 3개      | **이음새 없는 큰 실내 방 하나**(같은 벽지·마루 연속)                        |
| 카메라    | 구역 자석 스냅 + 플릭 페이징 | **자유 팬** — 관성만, 아무 데서나 멈춤(스냅·페이징 제거)                    |
| 배치      | 대청 전용(스코프 컷)         | **방 전폭 꾸미기**(single-zone world → 기존 x 0~100 = 전폭, 무마이그레이션) |
| 미니맵    | 구역 점 3개                  | 제거(자유 팬이라 무의미) — 위치 인디케이터는 후속 검토                      |
| HUD       | TODAY·소원·전체화면          | **TODAY 배지·전체화면 버튼 제거**, 소원 유지                                |
| 입장      | 재방문 0.5s(체감 실패)       | **매 진입 좌→중앙(신위) 팬 상시 가시화**                                    |
| 사랑방    | 후원 구역                    | 큰 방 오른편 영역(실내 문법과 정합) + 아바타 렌더 수복                      |
| 도트(안4) | 검수 대기                    | **CEO 보류** — 재론 시점 미정                                               |

기술 골자: zones 메커니즘은 **단일 구역(0~240)**으로 유지(경계 자체가 소멸=이음새 0),
벽지·바닥은 **가로 타일(repeat-x)** 렌더로 임의 폭 확장. 마당 실외 에셋 4장은 보관(후속
"앞마당 씬" 재론용). 확산(G-4)은 스타일 확정 전 **계속 동결**.

---

## 부록 B. 안2.2 「큰 방 v2 — 거니는 신당지기」 (2026-07-30 CEO 2차 검수 반영)

### 검수 결과 진단 (실측 근거)

| CEO 지적                                 | 실측 원인                                                                                                                                                                                 | 조치                                                                                                                                 |
| ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| ① 입장(걸어 들어오기)이 여전히 안 보인다 | 팬은 **정상 발화**(camX 0→70, 1100ms). 그런데 큰 방이 **균일 반복 무늬**라 70%를 움직여도 랜드마크가 없어 "이동"으로 지각되지 않는다. 3구역 때는 실외 대문→실내 제단이라 명백했던 것      | 방 구성에 **좌측 문(분합문)** 랜드마크 신설 + **신당지기가 문에서 제단까지 실제로 걸어온다**(연출의 주체를 카메라에서 캐릭터로 이동) |
| ② 오른쪽에 세로줄                        | `repeat-x` + `background-size: auto 100%` → 타일 폭이 소수점(밴드높이×종횡비)이라 **반복 경계마다 서브픽셀 이음선**. 크로스페이드로 픽셀은 맞췄어도 브라우저 반복 렌더 단계에서 선이 생김 | **반복 폐지.** 미러 타일링으로 구운 **와이드 무라 1장**(경계 픽셀 공유 = 수학적으로 불연속 0)                                        |
| ③ 좌우 이동 좋다                         | 자유 관성 팬 유지                                                                                                                                                                         | 변경 없음                                                                                                                            |
| ④ 방 확장 + 움직이는 캐릭터              | —                                                                                                                                                                                         | 폭 240% → **320%**, 신당지기 보행 프로토타입                                                                                         |

부수 발견(스크린샷): 말풍선이 world 안에 있어 팬하면 화면 밖으로 잘리고 「꾸미기」와 겹친다 → **HUD 고정**으로 이관.

### 방 구성 (폭 320% = 3.2화면)

```
 world x   0 ────────── 90 ─────────── 210 ─────────── 320
          [ 문간 ]      [ 제단·신위 ]    [ 가족 사랑방 ]
          분합문        중앙 무대        우측 코너
          입장 시작점   기본 카메라      좌석 반원
```

- 배치(꾸미기)는 **전 구간** 자유 — 넓어진 만큼 신물 지면 확대(수익 축)
- 카메라 기본 위치 = 제단 중앙, 입장은 문간에서 시작

### 신당지기 보행 (프로토타입 = 이번 테스트 범위)

- **문법**: 좌정 主神 초상 오브가 바닥 위를 좌우로 거닌다. 보행 = ①x 왕복 ②상하 바운스(2보 주기) ③방향 전환 시 좌우 반전 ④접지 그림자 동조
- **구현 원칙**: CSS keyframes만(rAF 상주 금지, ARCH §5) — `translateX` 왕복 24s infinite alternate + 자식에 바운스·플립. 컴포지터 전용
- **입장 연동**: 진입 시 문간(x≈8%)에서 제단 옆(x≈45%)까지 **한 번 걸어온다**(1.8s), 이후 상시 배회로 전환
- **상호작용**: 탭 → 걸음 멈춤(animation-play-state) + 말풍선 + 기존 사운드·파티클, 2.5s 후 재개
- **접근성**: `prefers-reduced-motion` 시 배회 정지(제단 옆 정위치), 입장 걷기도 생략
- **에셋 0단계**: 기존 초상 재사용(절차적 보행). CEO 승인 시 2단계로 4프레임 걷기 스프라이트 생성

### 리스크 · 게이트

- **EffectsCanvas 폭 상한**: 현 클램프 1400px, 320%×520 = 1664px → 초과 시 파티클 좌표 어긋남. **클램프 상향 + 버퍼 픽셀 예산(≤8Mpx) 검증 필수**(과거 흰화면은 2^25px=33Mpx 폭주였음 — 8Mpx는 1/4)
- 무라 용량: 미러 타일링 산출물 ≤300KB 목표(webp q78)
- 원복: 시드 `- 'zones' - 'width'`(단일 무대) / `SCROLL_SHRINE_V1=false` / 보행은 `GAMEFEEL_V1` 게이트

---

## 부록 C. 안2.3 「제단·단상 정립 + 신위 회전」 (2026-07-30 CEO 3차 검수)

### ① 세로줄 — 진짜 원인 확정 (픽셀 실측)

무라 이미지를 canvas 로 열어 열 평균 휘도를 분석한 결과:

| 대상             | 인접 열 차이 상위                                        | 중앙값 | 미러 축 대칭성                  |
| ---------------- | -------------------------------------------------------- | ------ | ------------------------------- |
| room-wall-mural  | **x261=105.0 · x1787=103.9 · x2309=104.6 · x3835=103.9** | 0.343  | 1024/2048/3072 = 0.28/0.22/0.28 |
| room-floor-mural | 최대 4.15                                                | 0.254  | 0.22~0.25                       |

- **미러 이음새는 완벽하다**(대칭 차이 0.2대) → B파 수정은 정상 작동. 그러나
- **원본 벽 타일 안에 Δ105 하드 세로 경계가 박혀 있다**(x=261). 미러 반복으로 1787·2309·3835 에
  같은 경계가 재현되고, 렌더 스케일(0.445)에서 뷰포트 ~140px·~374px 에 나타난다 —
  CEO 스크린샷의 선 위치(~380px)와 일치.
- **처방**: 무라 굽기 파이프라인에 **하드 에지 검출·페더링** 단계 추가(중앙값의 N배를 넘는 열을
  ±10px 선형 보간으로 완화). 목표 = 상위 Δ ≤ 20, 미러 대칭성 유지, 톤 무변경.
  ⚠️ 교훈: 이음새(tiling)와 **그림 안의 경계**는 다른 문제다. 다음부터 두 지표를 함께 본다.

### ② 입장 걷기 속도

문간→제단 1800ms 는 "지나가 버린다" → **3600ms**(도착 후 배회 승계는 그대로). 배회 한 구간도
24s → **30s** 로 늦춰 거니는 결을 맞춘다.

### ③ 제단·단상 정립 (CEO: "신이 있고 아래 단상이 있어야 하는데 어색하다")

기준: **달집 마당(`daljip`) 테마의 legacy `room.webp`** — 상단 제단이 그림으로 잘 표현된 그 느낌.
(daljip 은 stage 미보유 = 완성 일러스트라 제단이 배경에 그려져 있다.)

- 반가 큰 방에 **제단 = 2단 구성**으로 재정립: 상단 **제단 상판**(공물·촛대 놓는 면) + 하단
  **단상(壇上, 계단형 대)** — 신위는 단상 위에 서고, 제단이 그 앞·아래로 받친다
- 신위 세로 정합: 현재 신위 `bottom:50%` 와 제단 `y47` 이 겹쳐 "떠 있는" 느낌 → 신위 발이
  단상 상면에 접지하도록 y 축 재배치
- **방 높이 확대**: `min(56vh, 480px)` → `min(72vh, 620px)` (CEO "상하단으로 조금 더 길어도 됨").
  세로가 늘면 제단·단상·신위의 위계가 살고 걸이/벽/바닥 존도 넉넉해진다

### ④ 신위 탭 → 한 바퀴 회전 (CEO: "실제 애니메이션처럼 자연스럽게")

**설계: 2D 턴어라운드(turnaround) 5국면** — 회전의 자연스러움은 rotateY 하나로는 안 나온다
(180°에서 좌우 반전된 정면이 보여 "종이 뒤집기"가 된다). 프레임을 갈아 끼워야 한다.

```
0ms      정면(base)      ─ 아우라 응축 · 그림자 살짝 수축
280ms    측면(side)      ─ 폭 압축(scaleX .82) · 치맛자락 뒤따름
620ms    뒷면(back)      ─ 아우라 최대 · 옷자락 플레어
960ms    측면(flip)      ─ scaleX(-.82)
1300ms   정면(base)      ─ 착지 스쿼시(1.04→1) · 종·바라 사운드 · 파티클 버스트
```

- 이징: 전체 `cubic-bezier(.34,.02,.2,1)`(돌기 시작이 느리고 중반 빠름 = 회전 관성)
- 보조 연출: 발밑 접지 그림자가 회전 위상에 따라 좁아졌다 넓어짐 / 옷자락 트레일(잔상 2겹,
  opacity 0.18) / 회전 중 광원 스침(하이라이트 스윕)
- **에셋**: 신위별 `side.webp`·`back.webp` 2장. **시범 2신위**(선녀신·용왕신)만 먼저 굽고
  검수 통과 후 17신위 확산(장당 ≈$0.03, 전체 34장 ≈$1.1)
- **폴백(프레임 없는 신위)**: rotateY 회전 + 90° 근처 모션블러·아우라 증폭으로 뒷면 부재를 가림.
  프레임이 있는 신위는 자동으로 상위 경로 사용
- 탭 잠금(재생 중 재탭 무시), `prefers-reduced-motion` 시 회전 생략(기존 탭 반응만)
- 모든 CSS 는 `app/shrine-scene.css` 에 — **styled-jsx 금지**(부록 C 직전 P0 교훈)

---

## 부록 D. 안2.4 (2026-07-30 CEO 4차 검수)

CEO 지시 원문: "세로줄 그대로 / 입장속도 아직 빠름 / 신 클릭효과 좀더 자연스러운 효과를 위해
여러장으로 만들어줘, 돌 때 빛나는 효과도 살짝 넣어주고 / 가족들도 위치를 바꿀 수 있게 설정해줘"

### ① 세로줄 — 3라운드 동안 **엉뚱한 대상을 쫓고 있었다**

세로줄의 정체는 서브픽셀 아티팩트가 아니라 **미러 타일링이 만든 구도**였다.
`[T|flop(T)|T|flop(T)]` 는 이음새를 수학적으로 0 으로 만드는 대신 무라 중앙에 완벽한 거울축을
남긴다. 방 폭 320% 중 한 화면이 100% 라 그 축이 화면 안에 들어오고, 좌우가 통째로 뒤집힌 벽 +
균등 간격의 굵은 기둥은 사람 눈에 "방"이 아니라 "무늬"로 읽힌다.

**측정이 실패를 못 잡은 이유 2가지 (진짜 교훈)**

| 결함                     | 내용                                                                                                                                                                                |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 게이트가 구조적으로 통과 | `EDGE_DETECT_ABS=20` 인데 `EDGE_ACCEPT_MAX=20`. Δ≤20 은 **검출조차 안 되므로** 파이프라인은 항상 "19.x = 합격"으로 수렴한다. 실측 19.19 는 성과가 아니라 **검출 임계의 바닥**이었다 |
| 잰 해상도가 틀렸다       | 원본에서 재고 사람은 축소본을 본다. 실측 원본 Δ19.2 → 0.445 축소 후 Δ**28.5** 로 **오른다**(페더 램프가 다운샘플 커널보다 좁으면 화면에서 다시 계단으로 뭉친다)                     |

**처방: 가변 세그먼트 퀼팅** (`stage-banga-mural.mjs`, API 0회)

- 미러 교대 폐기. 타일을 **길이가 제각각인 조각**으로 잘라 이어붙인다. 자르는 자리는
  ①국소 평탄(기둥·먹선 위가 아님) ②이음매 쌍끼리 RGB 일치(≤`SEAM_EPS`) 인 열만.
- 랩(끝↔처음) 연속성은 **요구하지 않는다** — 무라는 `object-cover` 단일 stretch 라 그 경계가
  화면에 없다. 그 제약을 버린 만큼을 구도 자유도로 쓴다.
- 조각을 **뒤집지 않는다**. flop 을 섞었더니 인접 조각이 정·역상이 되어 국소 거울축이 재발했다
  (1차 시도 대칭 0.696).
- **막다른 길 고정점 가지치기** 필요. 한 수 앞만 보면 "이어지는 끝"이 다시 막다른 시작으로만
  이어져 40회 시도가 전부 실패한다(실측 a=715~794 구간).
- `SEAM_EPS` 는 스윕으로 정했다: 1.6 이면 이어갈 **순환이 아예 없어** 계획 실패 → 미러 폴백.
  3.0 부터 성립하고 실제 최대 이음매가 정확히 3.00 이다.

**새 게이트 3종** (종전 게이트는 전부 통과시켜 놓고 실패했다)

| 게이트     | 정의                                                                              | 실측                             |
| ---------- | --------------------------------------------------------------------------------- | -------------------------------- |
| ★좌우 대칭 | 한 화면 창 안 좌우 상관의 최대값 ≤0.55                                            | 미러 0.761 → 퀼팅 **0.372** PASS |
| ★하드 에지 | **렌더 스케일**에서, **타일 1장을 기준선**으로 "조립이 원본에 없던 선을 더했는가" | 조립이 더한 몫 **+1.2** PASS     |
| 이음매     | 랩 제외, 인코딩 전 ≤2·`SEAM_EPS`                                                  | 최대 2.86 (webp 후 1.26) PASS    |

> 절대 임계(Δ≤20)를 축소본에 걸면 **영원히 불합격**이다 — 한지 위 먹빛 기둥은 그 자체로 Δ80+ 이고
> 그건 아티팩트가 아니라 그림이다. 지울 대상이 아니라는 것이 이번 라운드의 핵심 판단이다.

마루 무라는 퀼팅 대상이 아니다(이음매 후보 열 부족 → 미러 폴백 유지). 축소 후 Δ5.5 로 3라운드
동안 민원이 0건이었고, 그 값이 곧 `HARMLESS_RENDER_DELTA` 의 근거다.

### ② 입장 걷기 — 상수가 아니라 **배회 속도에서 파생**

4차 지시의 진짜 원인은 "3600ms 가 짧다"가 아니라 두 속도가 따로 놀았다는 것이다.

- 안2.3: 입장 37%p/3600ms = **10.3%p/s**, 배회 28%p/30000ms = **0.93%p/s** → 입장이 배회보다 **11배** 빠름
- 도착 직후 같은 인물이 11배 느려지니 입장을 아무리 늘려도 "지나가 버리는" 걸음으로 읽힌다

`entranceMsFor(from, plan, ratio=4)` 가 배회 속도에서 길이를 계산한다(현 구성 **≈9.9s**).
상수로 두는 한 한쪽만 조정돼 다시 어긋나므로 파생으로 못 박고 회귀 테스트를 걸었다.

### ③ 신위 회전 — 5국면 → **9국면** + 회전 발광

`0°(base) → 45°(q45) → 90°(side) → 135°(q135) → 180°(back) → 225·270·315°(반전 재사용) → 360°(착지)`

- **굽는 건 4장**(정면은 DB `sprite_url`), 그중 side·back 은 안2.3 에서 이미 구웠으므로 신규는
  `q45`·`q135` 뿐. 180° 초과 3국면은 전부 `scaleX(-1)` 재사용이라 에셋이 늘지 않는다.
- 국면 시각은 등간격이 아니라 **smoothstep 의 닫힌 역함수**로 뽑는다 — 각도가 smoothstep 을
  따르므로 시작이 느리고 뒷면에서 최고속, 정면으로 감속 착지. 총 길이 1480ms 유지.
- **등급 3단**: 4장 다 있으면 9국면 / side·back 만 있으면 종전 5국면(A파 값 그대로) / 없으면 rotateY.
  즉 **중간각을 굽기 전에 배포해도 회귀가 0** 이다(테스트로 고정).
- 발광은 버튼에 건 `filter: drop-shadow` + 미세 `brightness`. drop-shadow 는 **그 순간 보이는 겹의
  알파**를 따라 번지므로 국면마다 실루엣이 저절로 맞고 사각형 섬광이 될 여지가 구조적으로 없다
  (종전 `deity-turn-sweep` 은 마스크가 정면 1장이라 프레임 교체 등급에서 빛이 몸 밖으로 샜다 →
  폴백 전용으로 좁혔다). 최대 α0.42 · brightness 1.06 — "살짝" 이라는 지시에 맞춘 상한.

**⚠️ 프롬프트 교훈 — 중간각은 방향을 안 주면 정면으로 수렴한다**

`q45` 를 "정면에서 45도 돌려라"로 두 번 구웠지만 매번 base 와 육안 차이가 거의 없는 그림이
나왔다(회전 앞 3분의 1이 정지처럼 보이고 side 로 넘어갈 때 튄다). 해결은 **기준을 바꾸는 것**이었다 —
참조 이미지가 side 이므로 "그 측면을 카메라 쪽으로 절반 되돌려라"가 훨씬 강한 신호다.
`side` 는 처음부터 "faces the viewer's right"로 방향이 박혀 있었고 그래서 잘 나왔다.

현 상태: **용왕신 q45 양호**(관·얼굴·상체가 우측으로 확실히 회전), **선녀신 q45 는 여전히 약함**
(머리숱이 대칭·풍성해 회전이 가려진다). 나머지 각(side·q135·back)은 두 신위 모두 양호하고
**회전 방향이 일관**되어 반전 재사용이 성립한다. 17신위 확산 전에 선녀신 q45 판단 필요.

### ④ 가족 좌석 이동

`shrines.hall_seats jsonb` 컬럼 1개. 키 없는 좌석 = 자동 반원 배치라 기본값 `{}` 만으로 기존
화면이 100% 그대로다(데이터 마이그레이션 0). 서버 재클램프·`.is('family_member_id', null)`·
등급 게이트는 기존 배치 저장 규약을 그대로 따른다.
마이그레이션 `20260730_family_hall_seats.sql` 은 **작성만 하고 미적용** — 배포 순서는
마이그레이션 → 코드(읽기는 `select('*')` 라 미적용 DB 에서도 죽지 않고 저장만 실패한다).

---

## 부록 E. 안2.5 (2026-07-30 CEO 5차 검수)

CEO 지시 원문: "세로줄 아직있음 / 회전 기존과동일한 움직임(부자연스럽고, 머리몸따로 돔) /
가족 좌석은 전구역 아무곳이나 배치가능하게 셋팅해줘"

### ① 세로줄 — 벽은 고쳤는데 **마루를 안 고쳤다**

안2.4 에서 벽 무라는 퀼팅으로 거울축을 없앴지만(0.761→0.372), 마루는 **미러 폴백을 그대로
남겼다**. 근거는 "인접 열 차이 Δ4.17 = 육안 무해 · 3라운드 동안 민원 0건"이었고, 그 판단이
틀렸다. Δ 가 작아도 **미러 축에서 나뭇결이 좌우로 접히는 구조적 대칭**은 그대로 보인다
(마루 대칭 상관 0.910 실측). 숫자가 아니라 구도가 문제라는 점은 벽과 **똑같았는데**,
벽에서 얻은 교훈을 마루에 적용하지 않은 것이 이번 재발의 직접 원인이다.

**처방: 크로스페이드 조립**(세 번째 모드). 마루는 매끈해서 퀼팅 절단 후보의 짝이 서지 않는다
(고정점 가지치기 결과 0). 그래서 뒤집지 않고 **같은 방향으로 겹쳐 깔고 겹침 구간(160px)을
smoothstep 으로 섞는다**. 겹침 안에서 같은 타일의 다른 위상이 섞여 경계가 사라지고,
뒤집지 않으니 대칭축도 생기지 않는다. 선형 혼합은 겹침 양끝에서 기울기가 꺾여 그 자리가
다시 선이 되므로 반드시 smoothstep 이어야 한다.

실측: 이음매 1.18(내부 p95 2.72 안 = 불가시) · 렌더 스케일 Δ 5.5 → **4.6** · 대칭 0.910 → 0.837.

**게이트 수정 — 대칭에 절대 임계를 걸면 안 된다.** 지표가 창 폭에 지나치게 민감하고
(같은 벽 무라가 창 1280 에서 0.372, 창 819 에서 0.653), **타일 원본 자체가 이미 대칭이라**
(마루 0.652 · 벽 0.864 — AI 가 대칭 구도로 그렸다) 어떤 조립도 0 에 못 간다.
그래서 묻는 것을 바꿨다: "충분히 비대칭인가"가 아니라 **"우리가 버린 미러 조립보다
나아졌는가"**. 같은 자로 재는 단조 기준이라 창 폭을 어떻게 잡든 방향이 뒤집히지 않는다.
(벽 0.372 < 기준선 0.760 · 마루 0.837 < 기준선 0.907 — 둘 다 PASS)

### ② 신위 회전 — 등급은 정상, **프레임 내용**이 문제였다

9국면(full) 판정·에셋 4장 서빙 모두 정상이었다. 그런데도 "기존과 동일"로 읽힌 이유는
중간각 프레임이 **몸만 돌고 얼굴은 카메라를 향하고 있었기** 때문이다. 각도 시퀀스가
몸 0→45→90, 머리 0→**0**→90 이 되어 연속 재생하면 정확히 "머리와 몸이 따로 돈다".

원인은 모델의 기본 편향(얼굴을 보여주려 한다)이고, 처방은 한 문장이다 —
`the head turned by the same angle as the torso and the gaze going off to the side, not at the viewer`.
**시선을 카메라 밖으로 보내라**고 명시하는 것이 핵심이다. 재생성 후 두 신위 모두 머리가
몸을 따라 돌고, 회전 방향도 side 와 일치한다(0°→45° 우측→90° 우측 프로필).

### 프롬프트 3연속 실패에서 얻은 규칙 (17신위 확산 전 필독)

| 시도 | 지시 방식                                       | 결과                                                |
| ---- | ----------------------------------------------- | --------------------------------------------------- |
| 1    | "정면에서 45도" (방향 없음)                     | **정면으로 수렴** — base 와 구분 불가               |
| 2    | 실루엣 단서 + "CRITICAL / must NOT be mistaken" | **캐릭터 시트**(얼굴 습작 여러 장) 생성, 폭 346→544 |
| 3    | side 와 같은 짧은 서술체 + 관찰 가능한 사실 1개 | 성공                                                |

**규칙: 설명을 늘리지 말고 관찰 가능한 사실을 하나 더한다.** 메타 지시문("CRITICAL",
"must NOT be…")은 그림이 아니라 지시서를 그리게 만든다. 방향·머리 각도·옷깃 치우침처럼
**보이는 것**으로만 쓴다.
