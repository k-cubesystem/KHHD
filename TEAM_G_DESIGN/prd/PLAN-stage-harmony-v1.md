# PLAN — 「한 붓의 무대」: 배경·살림 조화 재설계 v1

작성 2026-08-09 밤 · 발단: CEO 실기기 검수 반려(스크린샷 3장) — "배경이미지와 선반·제단·간판이
어우러지지 않는다. 위에 놓이는 것과 뒤의 배경이 설계가 달라 각자 논다. 서로 설계를 합쳐서
잘 어우러지게 배경을 다시 설계하라. 테마는 그대로."

## 0. 진단 — 따로 노는 원인 3가지

| #   | 원인                    | 증상(스샷)                                                                                                               |
| --- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 1   | **그림 속 가구의 경쟁** | 중앙 뮤럴에 배경 자체 가구(책장·선반·기물)가 그려져 있고 그 앞에 제단·단상 스프라이트가 또 놓임 — "방 안에 방을 또 차림" |
| 2   | **화풍·광원 계보 분리** | 뮤럴=원화 재현(회화풍·자체 광원) vs 살림·현판=아이템 파이프라인(선명 윤곽·난색 채도·정면 위 광원) — 스티커로 읽힘        |
| 3   | **좌우 벽 밀도 공백**   | 선반장·의식각 자리 확보용 무가구 벽이 "완성된 빈 벽"이 아니라 "덜 그린 허연 벽" — 그 위 가구가 더 붕 뜸                  |

**공정 결함(재발 방지 핵심)**: 지난 회차 검수는 **뮤럴 단독**이었다. 사용자가 보는 화면은
살림·신위·현판이 얹힌 합성 화면인데, 그 화면으로 검수한 적이 없다. → 이번 회차부터
**판정 단위 = 합성판**(측정 설계 교훈 「지각 품질은 렌더 스케일에서·먼저 눈으로」의 확장).

## 1. 원칙 — 설계 통합 계약

1. **배경 = 건축과 공기만.** 벽·기둥·창(중앙만)·바닥·절제된 벽 장식(족자·문양)까지.
   **가구·기물은 전 구간(중앙 포함) 금지** — 가구는 전부 살림 레이어의 몫. 테마 정체성은
   가구가 아니라 건축·재질·빛·소품 디테일로 낸다.
2. **살림이 스타일 정본.** 생성 참조 = 테마 원화(색·세계 앵커) + **실제 살림 스프라이트
   3종(단상·제단 상판·사방탁자)**(화풍·광원 앵커). "이 가구들이 놓일 방을 같은 붓으로
   그려라"가 프롬프트의 척추. 광원은 살림 그림자와 일치(정면 위 부드러운 난색, 위→아래).
3. **자리를 아는 벽.** 표준 기하(선반장 왼벽 x 7·16·25·34 / 오른쪽 x 63.5·72.5 /
   의식각 x 88.75 / 제단 x 45~55)의 자리에 은은한 받침 — 벽 패널 리듬·바닥 톤 변화 수준.
   ⚠️ 좌표 정밀 지시는 프롬프트로 안 되므로 "일정 간격의 벽 패널/기둥 리듬"으로 유도하고,
   정밀 자리 요소가 필요하면 **결정론 렌더 레이어**(후속)로 얹는다 — AI에 좌표를 맡기지 않는다.
4. **좌우 벽 = 완성된 빈 벽.** 무창·무문 유지하되 창호 패널·재질 질감·은은한 명암 리듬으로
   "비웠지만 다 그린" 벽. 허연 미완 벽 금지.
5. **판정 = 합성판.** 뮤럴 후보 위에 실좌표·실스케일로 단상+제단+신위 1좌+가족 선반장
   3좌+의식각 현판+제물 소수를 합성한 QA판으로 육안 판정. 뮤럴 단독 판정 금지.
6. **변경 반경 최소.** 테마 16종 유지 · 기하/시드/좌표/코드 변경 0 · **뮤럴 webp 교체만**.
   구 뮤럴은 백업 보존(원복 레버). 카드용 room.webp 무접촉.

## 2. 실행 — 시범 게이트 방식 (앰비언트 v1~v5·씬 파노라마 전례 승계)

- **R1 파이프라인**: `scripts/shrine-assets/stage-theme-harmony.mjs` 신설(정본 후보).
  `stage-theme-scene.mjs`(현 정본)는 원복 레버로 존치. 참조 이미지 다중 주입(원화+살림 3종),
  C(중앙)→L→R 3샷 연속 생성·크로스페이드 조립·벽 62%/바닥 40% 슬라이스는 scene 방식 승계.
  **합성 QA판 생성기 내장**(기하 JSON 좌표로 살림·신위·현판 합성, 렌더 스케일).
- **R2 시범 1테마 = 반가** → 합성판 CEO 검수 → 반려 어휘를 프롬프트 표에 처방 후 재시안.
- **R3 GO 시 15테마 확산** → 전 장 합성판 육안 → 시드 무변경 배포(파일 교체) → 게이트
  (용량 테마당 ≤1.5MB·뮤럴 실재 테스트) → 프로덕션.
- 병행 검토(시범 결과 보고 결정): 선반장 노란 자리 매트 톤다운, 현판 명도 조정 등
  **살림 쪽 미세 보정**은 배경 재설계로 부족할 때만.

## 3. 프롬프트 규율 (반려 4연속 교훈 승계)

- 설명을 늘리지 말고 **관찰 가능한 사실**로만: "가구가 하나도 없는 방", "벽은 창호 패널이
  일정한 간격으로", "빛은 위에서 부드럽게 내려온다" 급. 메타 지시(CRITICAL 등) 금지.
- 반려 어휘는 테마별 identity 문장으로 축적(씬 파노라마 방식 그대로).
- 육안 규율: 후보마다 합성판 생성 → 사람 눈 판정. 수치 게이트는 보조.

## 3.5 진행 추기 (2026-08-10 새벽)

- **R1~R3(3샷 크로스페이드)**: R2 확정했으나 CEO 반려 — "3장으로 나누지 말고 와이드 통으로,
  자연스럽게 이어지게". 수치 게이트 통과와 무관하게 톤 계단·«여러 개의 방»이 사람 눈에 잡힘.
- **R4~R5(통짜 원샷) — 정본 경로 확정**: 21:9 · **4K**(6336×2688) 원샷 → 세로 크롭(수평선 60%
  배분) → ×0.54 수퍼샘플. 이음매 구조적 소멸·격자 왜곡 0·용량 423KB·확산 비용 테마당 1회.
  ⚠️ 1K 금지(세계 3.2화면×DPR3 ≈ 3700px에서 뭉개짐). 발밑 램프(liftFloorFoot) 기본 유지.
  파이프라인 `stage-theme-harmony.mjs` 기본 경로 = 통짜(`--three-shot`은 원복 레버).
- **CEO 2차 피드백**: "선반·신·간판이 붕 떠 있다 → 그림마다 맞추기 힘드니 **꾸미기에서 조절**하게
  하자" → 고정 살림 3종(가족 선반장·신위 무대·의식각) 오프셋 조절 기능 착수
  (`shrines.fixture_offsets` jsonb, 편집 드래그+신당별 저장, 제단 앵커·근접 배치 동반 이동).
  테마별 기본 좌표 재조정은 하지 않는다(사용자 조절이 정답).
- **상태**: 15테마 확산은 조절 기능 검수 후 CEO GO 대기. 연두 자리 매트 톤다운도 결정 대기.

### 추기 2 (2026-08-10 오전) — 4테마 세트·매트 제거·확산 함정

- **연두 매트 정체 = 크로마키 그린 스필**(코드 아님): shelf-sabang은 바닥 웅덩이 절단+색 교정,
  촛대·향로·석등은 절단 0의 색상 회전(hue 50~160→44°, 알파·명도 보존, 채도 상한=자산 자가
  95퍼센타일). 원본 백업 assets-src/shrine/backup-\*-20260810.webp. **라이브 배포 완료**(0f637df).
  잔여: altar-top 0.66%·파이프라인 despill 사각지대(자산별 opt-in 필요) — 별도 작업 칩.
- **추가 3테마 시범**(달집 r2·설빛 r3·용궁 r1, API 7회): 대비판·전폭 합성판으로 CEO 검수 중.
  육안 소견: 달집·용궁 강함, 설빛 담백(한색 방 vs 난색 살림 대비 최대 — 반려 시 어휘 처방 재생성).
- **확산 함정(파이프라인 반영됨·상세는 stage-theme-harmony.mjs 주석)**:
  ①수평선은 프롬프트로 못 잡는다 — 폭 크롭 가지+폐루프 재계산으로 60% 맞춤(업스케일 로그가
  1.00에 근접하면 수퍼샘플 여유 없음 → 재생성) ②수평선 검출은 "가장 센 가로선"이 아니라
  **"벽이 끝나는 마지막 가로선"**(굽도리 오인 방지) ③「밤·추위」는 밝기를 못 내린다 — 값 자체를
  관찰 사실로("그림 전체가 어둡다, 어떤 면도 ~보다 밝지 않다") ④한색 테마에 "난색이 없다" 금지
  (회색 상자화 — 한색은 종이·빛에, 나무는 따뜻하게) ⑤"매끈하다"류는 민짜 사각형 주문 — 재료의
  결을 관찰문으로 ⑥공통 척추의 「warmth」·「the paper panels」 두 단어는 테마가 갈아 끼움(용궁에
  한지 금지) ⑦테마 표에 pooled/lightFrom + LIGHT_BY_CODE(DB 실값) 확산 시 함께 채울 것
  ⑧반가 살림만 갱신할 땐 `--qa-only`(--assemble-only는 승인 바이트를 바꿈).

### 추기 3 (2026-08-10 오후) — 무대 규격 v3: 밀도·화질·크롭 동시 해결

**CEO 3차 피드백**: ①"벽·바닥만 있으니 퀄리티가 안 살아 — 슬라이드 더 길어도 되니 공간 활용,
단 육안 5항목은 지켜라" ②"모바일에서 4K인데 화질이 안 좋다" ③"배경이 전부 확대돼 나온다".

**진단(실측)**: ②③은 같은 뿌리 — 뮤럴이 기준 뷰포트(≈520px)의 밴드 비율(벽 4.33:1)로
잘려 있는데 실폰 밴드는 3.0~3.3:1 → `object-cover`가 세로 맞춤 후 **가로 20~25% 크롭+확대**
(반가는 구규격 6.4:1이라 최악). 저장본 3435px는 DPR3 소요(≈3.7~4.1k px)보다 작아
크롭과 겹치면 **1.3~1.4× 업스케일**. 4K는 생성 해상도일 뿐 저장·렌더가 깎아먹고 있었다.

**규격 v3 (해법 — 재생성 1회로 ①②③ 동시 해결)**:

1. **생성 16:9 · 4K**(21:9 폐기): 세로 여유를 확보해 벽(상부 헤드룸)·바닥(전경 심도) 모두
   전 기기 밴드 비율을 덮는다. 수평선 60% 폐루프·발밑 램프·통짜 원샷은 유지.
2. **렌더 = width-fit 고정**(object-cover 폐기): 벽 이미지는 밴드 **하단 앵커**(수평선=마루선
   접지 불변), 바닥은 **상단 앵커**, 가로는 항상 월드 폭 100%(크롭 0), 세로 잉여는 밴드가
   클립. 기기별 확대·크롭 소멸, 수평선 전 기기 고정.
3. **수출 2렌디션 + srcset**: 고해상(≈5.4k px, DPR3 수퍼샘플)+표준(2048, 저사양/저DPR).
   용량 예산 테마당 ≤2.5MB로 상향(srcset이 모바일 다운로드를 표준본으로 제어 — LCP 방어).
4. **밀도 규칙(육안 5항목 준수 하에)**: 가구 0 불변. 대신 —
   · **개구부 원경**: 칸 리듬 중 일부 칸이 열려 테마의 원경이 보인다(달집=마당 너머 달집더미와
   보름달, 설빛=설경, 용궁=심해 원경…). 선반장(x7~34·63.5~72.5)·의식각(88.75) 앞 벽은
   여전히 닫힌 차분한 벽 — 개구부는 그 사이 칸으로(정밀 좌표는 프롬프트 금지, 리듬으로).
   · **상부 밀도**: 서까래·보·단청 띠·(글자 없는) 장식 — 벽 상부 1/3이 이야기한다.
   · **바닥 변주**: 문양·돗자리·재질 경계 — 민짜 널 금지.
5. **월드 폭은 320% 유지 권고**: "슬라이드 연장"은 폭당 해상도를 희석(같은 원판을 더 늘려
   깖)하므로 v3에 안 섞는다. 연장이 꼭 필요하면 별도 회차에서 «건축 구획(문틀·기둥)으로
   경계가 정당화된 경관 베이 추가 생성» 방식으로(통짜 반려 취지 위배 없음).
6. **절차**: 시범 반가(현행이 구규격 최악 + CEO 기준방) → 검수 → 전 테마 재생성(라이브
   5테마 포함 — 어차피 ①밀도 반려 대상). 구본 백업 관례 유지.

### 추기 4 (2026-08-10 저녁) — 무대 기하 v4 「틀(壇)」

**CEO 4차 지시**: "신당을 여기 디자인했을 때처럼 각각 멋있는 **틀**을 만들어 주고, 액자 있는
부분에 신위를 배치해 줘. **마루바닥 사이즈를 좀 줄이고**. 틀 즉 제단을 더 크고 웅장하게,
그 제단 상 위에 아이템을 많이 배치할 수 있게." (레퍼런스 = 구 원화의 닫집·제단장)

**확정 규격 v4** — 기하·코드(오퍼스 I) / 틀 스프라이트·뮤럴 재슬라이스(오퍼스 J) 병행:

| 항목      | v3                          | v4                                      | 근거                                          |
| --------- | --------------------------- | --------------------------------------- | --------------------------------------------- |
| 벽 밴드   | 62%                         | **72%**                                 | 마루를 줄여 벽을 세운다                       |
| 바닥 밴드 | 40%                         | **30%**                                 | 겹침 2%p 유지                                 |
| 마루선    | y60                         | **y70**                                 | = 100 − 바닥 밴드 (바닥재가 벽지 위에 그려짐) |
| 살림      | 단상+상판 2종               | **틀(grand-altar) 1종**                 | 닫집·감실·제단이 한 장                        |
| 틀 상자   | 단상 y51 w44 / 상판 y58 w58 | **x50 y39 w50** (상단 y≈10 ~ 접지 y≈68) | 위·옆·아래로만 커진다                         |
| 제단 앵커 | 3점 45/50/55                | **2열 5점** (아래 추기 5)               | "아이템을 많이" · 틀 상판 실폭 안에서만       |
| 상판면 y  | 53.5                        | **2열 52.4 / 55** (추기 5)              | 앵커는 «다음에 놓을 자리»만 정한다            |
| 신위 접지 | 45.3                        | **45.3 (불변)**                         | 신위가 틀의 감실 안에 들어앉는 구도가 된다    |

- **시범 3테마**(반가·달집·설빛)만 틀. 13테마는 v3 살림 그대로 — **혼재가 정상 상태**다.
- **밴드는 렌더 상수**(`theme-stage.ts STAGE_BANDS`)라 시드에 실리지 않는다 → 코드 배포와 시드
  적용이 서로를 기다리지 않는다. 뮤럴은 width-fit 이라 구 규격 장도 가로 크롭 0 으로 살아남는다.
- **신수 배회 y61 → y69** (마루선−1 파생. 하드코딩 제거 — 다음 마루선 이동은 공짜다).
- **자산 계약(J)**: `public/shrine/stage/<code>/grand-altar.webp`, 레터박스 0(캔버스=내용물).
  기준 방 520×620 에서 위 상자(상단 y10~접지 y68)가 되려면 **세로/가로 ≈ 1.383** — 겉보기 세로는
  방 종횡비에 딸리므로 «기준 방에서 맞고 좁은 폰에서는 조금 낮다»가 정상이다(단상과 같은 성질).
  스프라이트 위에서 잰 세로 지점 셋:
  · **60.9%** — 감실 바닥(신위 발이 닿는 면. PODIUM_TOP_Y 45.3)
  · **75.0%** — 제단 앵커 줄(아이템 **중심**이 서는 y53.5)
  · **≈81%** — 제단 상판 **면**(앵커 + 아이템 반높이 3.6%p ≈ y57.1). 이 면이 앵커 줄보다 아래에
  있어야 물건이 상 위에 놓인 것으로 읽힌다.
  404 는 `hideOnError` 가 삼키므로 파손이 아니라 «살림 없는 빈 방»으로 난다.
- **알려진 사각지대**: 사방탁자 두 벌(가족 선반장·의식각)의 밑동 y62 는 v3 마루선 기준이라
  v4 에서 8%p 뜬다. **동결**한다 — 옮기면 진열 칸 앵커가 내려가 이미 진열한 아이템이 널에서
  떨어지고(배치는 절대 좌표) 스프라이트도 세로로 늘어난다. 뜬 만큼은 CEO 2차 지시로 만든
  신당별 조절(`fixtureOffsets.familyShelf.dy` ±14)이 덮는다. 검수 후 재론.

### 추기 5 (2026-08-10 밤) — 기하 v4 마무리 정렬 (틀을 실제로 구운 뒤)

틀 3종을 굽고 합성판(CEO 판정본)을 만들자 추기 4 의 앵커 확정안이 **그림과 어긋난 것**이
드러났다. 틀은 w50 이고 상판은 그 72% 라 상판의 세계 폭이 **x44.4~55.6** 뿐이다:

- 바깥 두 점(38·62)은 상판 **밖 허공**에 떨어졌다.
- 안쪽에 다섯을 한 줄로 세우면 간격 2.65%(폰 44px)라 아이템(0.88배 = 82px)이 절반씩 겹쳤다.

처방은 원화(달집)의 진설 그대로 — **깊이 2열**로 나누고 제단층만 작게 한다. 합성판에 구워져
판정을 받은 값이 정본이다:

| 항목             | 추기 4 안             | 확정(합성판)                                       | 근거                                              |
| ---------------- | --------------------- | -------------------------------------------------- | ------------------------------------------------- |
| 제단 앵커        | 한 줄 5점 38~62·y53.5 | **뒷줄 46.25/50/53.75·y52.4 · 앞줄 48.1/51.9·y55** | 전부 상판 실폭·존(x24~76·y48~58) 안               |
| 앵커 id          | far-left/right 신설   | **뒷줄이 v3 3점 id 승계 + `altar-front-*` 2점**    | 저장된 anchor_id 가 «모르는 자리»로 안 떨어진다   |
| 제단층 배율      | 0.88                  | **0.49** (틀 무대 전용)                            | 폰 상판 137px ÷ 3 ≥ 아이템 45.8px                 |
| 신위 머리 여백 y | 12(전 테마 공통)      | **반가 26.9 / 달집 27.9 / 설빛 28.3**              | 그 틀의 감실 윗턱 — 신위가 감실 «안»에 들어앉는다 |

- **저장 배치 이동 0 의 근거가 바뀌었다.** 종전 설명("상판면 53.5 불변")은 더 이상 성립하지
  않는다. 진짜 근거는 **배치가 절대 좌표**라는 것이다 — 앵커는 «다음에 놓을 자리»만 정하므로,
  앵커가 어디로 가든 이미 놓인 신물은 어제 자리에 그대로 있다.
- **배율·머리 여백은 렌더 상수**다(밴드와 같다). 시드에 안 실리고 `lib/domain/shrine/stage.ts`
  의 `depthScale` · `deityHeadRoomY` 가 정본이며, 값 표는 기하 JSON `grandAltar` 블록에 있다.
  분기는 각각 **한 곳뿐**이다 — 렌더에서 따로 곱하지 말 것.
- **남은 어긋남**: 반가 틀의 겉보기 상자가 y7.98~70.02 로, 자산 계약(상단 y10·접지 y68 ±2)을
  양끝에서 0.02%p 벗어난다(AR 1.4795 vs 계약 1.383). 달집·설빛은 통과. 감실 바닥·상판 앞턱
  같은 **하중선은 맞아 있고**(리패드가 맞춘다) 상자만 조금 큰 것이라 판정은 J·CEO 몫이다.
- **알려진 사각지대(신설)**: 제단층은 y 와 무관하게 z 가 14 고정이라, 2열의 앞뒤가 **DOM 순서**
  (배치 생성 순)로 정해진다. 앞줄에 먼저 놓고 뒷줄을 나중에 놓으면 뒷줄이 앞을 덮는다.
  아이템이 0.49 로 작아져 겹침 자체가 드물어 이번 패스에서는 두지만, 확산 전에 재론.

### 추기 6 (2026-08-10 밤) — 기하 v4.1 「틀 확대·마루 추가 축소」

**CEO 5차 지시**: "틀 사이즈를 더 크게 웅장하게 만들어주고 마루를 좀 줄여도 될 것 같아."

| 항목             | v4                         | **v4.1 확정**                              | 근거                                             |
| ---------------- | -------------------------- | ------------------------------------------ | ------------------------------------------------ |
| 벽 / 바닥 밴드   | 72 / 30                    | **75 / 27** (겹침 2%p)                     | 마루를 한 번 더 줄인다                           |
| 마루선           | y70                        | **y73**                                    | = 100 − 바닥 밴드. 신수 배회 y69 → **y72**(파생) |
| 틀 상자          | x50 y39 w50 (높이 55.6)    | **x50 y38.5 w60** (상단 y3.5 ~ 접지 y73.5) | 폭 +20% · 세로 +26%. **접지가 마루선에 앉는다**  |
| 제단 앵커        | 46.25/50/53.75 · 48.1/51.9 | **45.7/50/54.3 · 47.85/52.15** (y 그대로)  | 상판 실측 폭에서 재파생(세계 x43.4~56.6)         |
| 제단층 배율      | 0.49                       | **0.54**                                   | 390폰 상판 161px ÷ 3, 안쪽 여백 5%               |
| 신위 머리 여백 y | 26.9 / 27.9 / 28.3         | **25.3 / 23.9 / 25.7** (반가/달집/설빛)    | 커진 감실의 윗턱 — 신위도 그만큼 커진다          |
| 제단층 z         | 14 고정                    | **13~15 (y 파생)**                         | 2열 앞뒤가 DOM 순서로 정해지던 사각지대 수복     |

- **상자 높이는 «정하는» 값이 아니라 «재고 나서 적는» 값이다.** 시드가 고정이고 두 생명선(감실
  바닥 45.3 · 제물 밑변 60.98)을 지키면 자유도가 없다: `상자 높이 = 15.68 / (f₂ − f₁)`. 착수 때
  어림한 y6~71(높이 65)은 (f₂−f₁)=0.241 을 가정한 값인데 r5 세 장이 0.221~0.225 로 나와 **높이 70**이
  됐다. 결과가 더 낫다 — 접지가 마루선에 내려앉는다(v4 는 접지 68 · 마루선 70 이라 2%p 벽 속에 박혀
  있었다). 「더 웅장」의 최종 수치는 **겉보기 면적 v4 대비 1.38~1.50배**(반가 1.38 · 달집 1.50 ·
  설빛 1.47), 기준 방 520×620 에서 260×384px → **312×436~451px** 이다.
- **측면 리패드 신설**: 세로 리패드는 두 랜드마크를 함께 옮길 뿐 «사이 거리»를 못 바꾼다. 그 거리를
  정하는 것은 (f₂−f₁)·AR 하나이고 AR = 캔버스 세로/가로이므로, **좌우 투명 여백**이 그 유일한 레버다.
  ⚠️ 세로 패드를 «나중에» 계산하면 그 몫이 AR 을 다시 밀어 올려 상자가 계약을 벗어난다(달집에서
  실제로 75.3 이 나왔다) — 그래서 `planSidePad` 가 세로 패드까지 넣고 한 번에 고른다.
- **자동 검출은 또 헛짚었다**: 감실 바닥은 세 테마 모두 38~42%(실제 59~64%), 설빛 상판은 앞턱 대신
  **뒷턱**을 잡았다. 육안 실측(0.5% 눈금 확대판)이 정본이고 라운드마다 다시 잰다(`THEMES[].eye[round]`).
  ⚠️ 최종 `marks.boardFront` 가 자동값을 다시 읽어 **육안값을 조용히 버리던 버그**를 함께 고쳤다.
- **뮤럴 재슬라이스**: `--band 75/27 --horizon 70` 로 r72 → **r75**. 벽 AR 2.509 → **2.480**(폭 크롭
  1.1%), 바닥 AR 5.90. 벽 밴드 최솟값(360×800)이 2.607 이므로 **전 기기 가로 크롭 0**, 여유 행 107행
  (5.1%). ⚠️ `object-cover` 는 언제나 밴드를 채우므로 «위가 빈다»는 없다 — 실패 모드는 **가로 크롭
  (=확대)**이다. 13테마(구 뮤럴 AR 4.326)는 밴드가 커진 만큼 가로 크롭이 37%→40%로 늘고 바닥은
  오히려 크롭 0 이 된다(밴드 AR 7.24 > 뮤럴 6.71). 용궁 대비판으로 회귀 없음 확인.
- **사방탁자 동결 유지**: 밑동 62 는 이제 마루선보다 **11%p** 위다. 조절 손잡이(±14)가 아직 덮는다 —
  그 부등식이 계약 테스트에 박혀 있으니 마루선을 한 번 더 내리려면 여기부터 본다.
- 산출: `assets-src/shrine/grand-altar-v41/{code}/` (center·wide·좌우·기기 3종·**v4 대비판**),
  구본 백업 `…/backup/{code}-v4-grand-altar.webp`. public 교체는 틀 3장뿐.

### 추기 7 (2026-08-10 밤) — 접지 수복 「마루 라인에 맞춰서 내려와야 해」

**CEO 실기기 검수**: "배경 적용 확인했어. 하지만 **틀과 선반들이 공중에 떠 있어. 마루 라인에 맞춰서
내려와야 해.**"

부양의 지분은 셋이었고, 원인이 서로 다르다. 하나만 고치면 나머지가 그대로 보인다.

| #   | 무엇            | 실측 부양               | 원인                                                             | 처방                                       |
| --- | --------------- | ----------------------- | ---------------------------------------------------------------- | ------------------------------------------ |
| ①   | 사방탁자 2벌    | **11%p** (전 기기 동일) | 밑동 62 동결(추기 4·6) — 마루선만 60→73 으로 내려갔다            | 밑동 = **마루선 파생**, 높이 불변 평행이동 |
| ②   | 틀 — 스프라이트 | 3.22 / 1.79 / −0.66%p   | 세로 리패드가 남긴 **하단 투명 여백**(그려진 받침이 위에서 끝남) | 세로 3구간 조각 늘림 + 트림                |
| ③   | 틀 — 렌더 규칙  | **6.7~9.7%p** (기기별)  | 구조물이 **폭 기준**이라 겉보기 세로가 방 종횡비에 딸려 옴       | 틀만 **세로 기준** 렌더                    |

- **①** `FSHELF_UNIT.top/bottom` 을 `STAGE_FLOOR_LINE_Y` 파생으로 바꿨다(20→31 · 62→**73**, delta +11).
  높이 42 는 불변이라 `objectFit:'fill'` 왜곡이 0 이고, 의식각(`RITUAL_HALL_UNIT`)은 그 값을 **승계**해
  두 벌이 다시 갈라질 길을 없앴다. 진열 칸 앵커(`seat:fshelf:`)가 함께 +11 내려가므로 **이미 진열해 둔
  아이템**은 마이그레이션 `20260810b_family_shelf_ground_shift.sql` 이 데려간다(작성만·미적용).
  판정은 박스가 아니라 **앵커 id + 옛 칸 좌표 정확일치**다 — delta(11)가 단 간격(10.08/8.82)보다 커서
  옛·새 박스가 겹치기 때문이다(박스 판정은 재실행 시 이중 이동). 정확일치는 옛/새 y 집합의 교집합이
  공집합이라 **재실행이 무해**하다. 그려진 발끝은 스프라이트 자체 여백 때문에 마루선보다 1.1%p 위에서
  끝나는데, 그만큼 더 내리면 맨 아래 칸이 벽 존(y≤60) 밖으로 나가 **거기서 멈춘다**(drop-shadow 가 덮는다).
- **②** 좌표로는 못 고친다. 시드의 자유도는 «배율·위치» 둘인데 지켜야 할 줄은 셋(감실 45.3 · 제물 밑변
  60.98 · 접지 73)이고, 셋 사이의 **픽셀 거리비**가 그림에 이미 박혀 있다. 계약이 요구하는
  (접지−상판)/(상판−감실) = **0.7662** 인데 실측이 banga 0.617 · daljip 0.765 · seolbit 0.822 —
  banga 는 상판 아래가 19% 짧다. 폭(w)을 키워 늘리는 길은 막혀 있다(닫집~감실 구간이 함께 자라
  w69 에서 처마가 y−3.9 로 잘린다). 그래서 랜드마크 두 줄로 끊어 **상판 아래 구간만** 늘렸다:
  `scripts/shrine-assets/stage-grand-altar-ground.mjs`(API 0회 · 행 단위 연속 워프라 이음매 0).
  · 배율 A(닫집~감실) **1.000 불변** / B(감실~상판) 1.053·1.098·1.011 / C(상판~접지) 1.308·1.100·0.942
  · 감실 바닥·상판 앞턱이 제자리라 **신위·제단 앵커·이미 놓인 제물이 하나도 안 움직인다**(추가 마이그레이션 0)
  · 산출 `grand-altar-v2.webp`(같은 이름 덮어쓰기는 캐시가 옛 그림을 준다) · 구본은 public 에 남는 원복 레버
  · 시드 y 38.5 → **37.22**(상자 y1.44~**73.00**) · 감실 윗턱 25.3/23.9/25.7 → **24.9/23.2/25.6**
- **③ 이번 회차의 가장 큰 발견.** 구조물은 «폭만 정하고 세로는 종횡비» 라, 기준 방(520×620)에서 71.56%
  인 틀이 390 폰(382×608)에서는 **53.6%** 로 줄어 접지가 마루선보다 **9%p(54px)** 위에 떴다. 즉
  스프라이트를 아무리 잘 구워도 **실기기에서는 뜬 채로** 보였다 — CEO 가 본 것의 대부분이 이것이다.
  틀의 계약이 전부 방 y 이므로 크기 기준도 방 세로여야 한다 → `GRAND_ALTAR_BOX_H = 2·(마루선 − 시드 y)`
  를 **세로**로 박고 가로는 종횡비에 맡긴다(`StageLayers`, 틀 구조물 한 종만). 기준 방에서는 두 규칙의
  결과가 같아 **회귀 0**, 폰에서는 틀 폭이 60% → 74~82% 로 넓어진다(「더 크고 웅장하게」와 같은 방향).
  짧은 창에서는 반대로 줄어 방 밖으로 넘치지 않는다 — 구 규칙은 넘쳤다.
- **판정 단위 = 합성판**: `assets-src/shrine/grand-altar-v42/{code}/`
  (`compare-device-390x844` · `compare-altar/shelf-before-after` · `ruler-center/shelf/hall` = 마루선 오버레이).
- **남는 것**: 13테마(v3 살림)는 여전히 폭 기준이라 같은 성질을 갖는다 — 단상·상판은 세로가 얕아
  체감이 작지만, 확산 때 틀 규칙을 함께 가져가야 한다. `fixture_offsets` 로 이미 부양분을 손수 내려둔
  신당은 **이중 하향**이 되므로 마이그레이션 주석의 개수 쿼리로 세고 사람이 판단한다(자동 초기화 금지).

## 4. 리스크

| 리스크                                             | 대응                                                                                     |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| 중앙 무가구화로 테마 정체성 약화(반가 서재감 소실) | 정체성을 건축·소품(창·족자·문양·재질)으로 이전 — 시범에서 판정                           |
| 살림 참조가 배경에 가구를 "복제"시킴               | 프롬프트에 "참조 가구는 화풍 기준일 뿐, 방은 비어 있다"를 관찰 사실로 서술 · 육안 게이트 |
| 화풍 통일이 원화 대비 격 하락                      | 원화를 색·세계 앵커로 계속 주입(팔레트 유지) — 격은 합성판에서 판정                      |
| 16테마 재생성 비용                                 | 시범 1테마 게이트로 흡수(전례 동일). 호출 ≈ 테마당 3~6회                                 |
