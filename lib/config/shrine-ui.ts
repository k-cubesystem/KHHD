/**
 * 신당 화면 구성 게이트 — CEO 6차 지시(2026-07-30)로 **감춘** 블록의 되돌림 스위치.
 *
 * 지시는 "빼 달라"였지만 둘 다 **계산·데이터는 그대로 살아 있다**. 지운 것이 아니라 가린 것이다.
 * 이유는 되돌림 비용에 있다 — 오행 값은 신물 카탈로그(DB)에 이미 박혀 있고 용신 판정은
 * 가이드바·공명 연출·소원 문구가 함께 쓴다. 화면만 걷어내면 이 파일의 한 줄로 돌아오지만,
 * 도메인까지 들어내면 마이그레이션과 시드를 되살려야 한다.
 *
 * 되돌리는 법: 해당 상수를 true 로 바꾸면 끝이다. 다른 파일은 손댈 것이 없다.
 */

/**
 * ① 「氣運 균형」 오행 막대 — 신당 하단 5행 게이지.
 *
 * CEO: "신당의 오행균형은 빼줘 별 쓸모가없어 나중에다시 기획할께" (2026-07-30)
 *
 * ⚠️ 끄는 것은 **막대 표시뿐**이다. `computeEnergy`(lib/domain/shrine/energy.ts)는 계속 돌고
 *    그 결과(용신·공명)는 아래 셋이 그대로 쓴다 —
 *      · 가이드바 「사주에 ○ 기운이 필요해요」 할 일
 *      · 신물 탭 시 오행 공명 파티클 + 토스트
 *      · 보관함 신물 배지의 오행 색
 *    그래서 energy 계산을 지우면 이 셋이 함께 죽는다. 지우지 말 것.
 */
export const SHOW_ENERGY_BALANCE = false

/**
 * ② 「테마 수집 N/M」 + 테마 칩 줄 — 신당 방 아래 가로 스크롤 칩.
 *
 * CEO: "테마수집도 빼주고 테마, 아이템등을 모아서볼수있게 신위처럼 버튼으로만들어줘" (2026-07-30)
 *
 * ⚠️ 칩 줄은 수집 현황이자 **테마를 적용하는 유일한 손잡이**였다. 그래서 감추기 전에
 *    적용 경로를 모아보기(/protected/shrine/collection?tab=theme)로 옮겨 두었다 —
 *    이 값을 true 로 되돌리면 적용 손잡이가 두 곳이 되지만 둘 다 같은 서버 액션
 *    (activateThemePack)을 부르므로 상태가 갈리지는 않는다.
 */
export const SHOW_THEME_COLLECTION = false
