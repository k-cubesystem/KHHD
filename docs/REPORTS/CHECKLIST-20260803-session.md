# 체크목록 — 2026-08-03 세션

이 세션에서 프로덕션(k-haehwadang.com)에 나간 것과, **사람이 직접 봐야 끝나는 것**을 모은다.
제가 기계로 확인할 수 있는 것은 전부 확인했고, 남은 항목은 실기기·계정·결제가 필요해 못 한 것들이다.

---

## 1. 배포된 것 (전부 프로덕션 반영 확인)

| 커밋      | 내용                                                                | 상태 |
| --------- | ------------------------------------------------------------------- | ---- |
| `b1374db` | 웹툰 서명 URL — 최적화 우회·TTL 900초·일괄 발급, 모르는 등급은 잠금 | ✅   |
| `8359a77` | 웹툰 운영 화면 — 회차 등록·본문 업로드·사연 회신·가림 되돌리기      | ✅   |
| `a777f14` | 의례 전환(A-1) + 신물 32종 확장                                     | ✅   |
| `d78e4d8` | 연출 계측(A-2) `?anim=1`                                            | ✅   |
| `2615746` | 스크롤 읽기 막대(A-3)                                               | ✅   |
| `8df7cdf` | 좌정 신위 표정 상태머신(B-1) + 이 체크목록                          | ✅   |

게이트: tsc 0 · eslint 0 · **jest 1,585** · build ✓ · CSS 게이트 **73종/68종** · DB 정책 7건 · 신물 46종

---

## 2. 사장님이 직접 봐야 하는 것

### 2-1. 연출 계측 (제일 먼저 — 5분)

```
https://k-haehwadang.com/protected/shrine?anim=1
```

- [ ] 좌하단 배지가 뜬다
- [ ] **초록 「연출 N종 구동 중」** 이면 정상 / **붉은 「N종이 죽어 있음」** 이면 사고
- [ ] 붉으면 배지를 눌러 목록을 열고, **「죽음」 줄에 뜬 이름**을 알려 주세요
- [ ] 같은 방법으로 `/protected/shrine/obangki?anim=1` 도 확인

> 「없음」 줄은 사고가 아닙니다 — 화면에 그 요소가 없다는 뜻입니다(배치 안 한 레이어 등).

### 2-2. 의례 전환 (A-1)

- [ ] 신당에서 **오방기 / 엽전 세 닢 / 백일기도** 아이콘을 눌렀을 때 화면이 툭 끊기지 않고 넘어간다
- [ ] 뒤로 가기에서도 마찬가지
- [ ] **화면이 멈추는 일이 없다** (1.2초 시한을 걸어 뒀지만, 실기기에서 한 번은 확인해야 함)
- [ ] 링크를 **길게 눌러 「새 탭으로 열기」** 가 여전히 된다

### 2-3. 8차 실기기 검수 (이전 세션에서 넘어온 것)

- [ ] **오방기** — 기가 낱개 5개가 아니라 **한 다발**로 보이는가 / 펄럭임이 도는가 / 풀이가 산문으로 읽히는가
- [ ] **척전** — 같은 갈림길로 **두 번째 던질 때** 연출이 다시 돌고 결과 카드가 뜨는가 (첫 판만 되고 두 번째부터 안 뜨던 버그가 있었음)
- [ ] **팻말 4장** — 좁은 폰에서 넷째가 잘리지 않는가
- [ ] **프로필** — 네 묶음(신당/나를 보는 일/웹툰/살림) 배치가 읽히는가

### 2-4. 웹툰 운영 화면 (신규)

마스터 계정으로 `/admin/webtoon`

- [ ] 회차 하나를 **초안으로** 만들어 본다 (공개 시각 비우고 저장)
- [ ] 컷 이미지 두어 장 올려 본다 → 진행 표시가 뜨고 미리보기가 그려지는가
- [ ] `/protected/webtoon` 목록에 **초안이 안 보이는지** 확인 (보이면 사고)
- [ ] 공개 시각을 넣고 저장 → 목록에 뜨는지
- [ ] 회차를 열어 **상단 진행 막대**(A-3)가 스크롤을 따라 차는가
- [ ] `/admin/webtoon/stories` — 사연이 있으면 상태 바꾸고 회신 한마디 저장

### 2-5. 좌정 신위 표정 (B-1, 신규)

`/protected/shrine` — 유대 L2 이상 계정에서

- [ ] **오늘 기도를 올린 뒤** 신위 얼굴이 흡족한 표정(bless)으로 바뀐다
- [ ] 백일을 시작해 놓고 **오늘 기도 전**이면 기다리는 표정(sad)
- [ ] 아직 시작 전이면 평상(기존 그림 그대로)
- [ ] **깨진 이미지가 안 뜬다** (표정 파일이 없는 신위가 있으면 여기서 드러납니다)
- [ ] 유대 L1 계정은 표정이 둘(평상·웃음)뿐이다

### 2-6. 신물 확장

`/protected/store` 신물 탭

- [ ] 신물이 **46종**으로 늘어 있다
- [ ] 각 카드에 **오행 배지 · 기운 수치 · 기도 갈래 · 전거** 넉 줄이 보이는가
- [ ] 이모지로 그려진다 (스프라이트가 없는 신규 32종은 이모지 폴백)

### 2-7. 가족 자리 확장 (2026-08-06 신규 — 가구 5종 + 세간 전반 가족 배정)

`/protected/store` 「시렁·세간」 갈래 → 구매 → 신당 배치 모드

- [ ] 상점 「시렁·세간」에 **제상 · 개다리소반 · 반닫이 · 문갑 · 보료** 5종이 실그림으로 보인다
- [ ] 하나 사서 배치하면 그 아래 **「+ 가족 지정」** 이름표가 뜬다 (보기 모드에서)
- [ ] 이름표를 눌러 가족을 지정 → 이름과 정령 색 점이 이름표에 남는다
- [ ] **시렁이 아닌 세간에도 된다** — 기존 병풍·연화방석·기억의 함에도 이름표가 떠야 한다
- [ ] 지정한 가족의 정령 오행 신물을 곁에 두면 **은은한 빛(축복)** 이 돈다
- [ ] 배치를 옮겨 저장해도 지정이 살아 있다 (저장마다 지정이 날아가던 함정의 회귀 확인)

### 2-8. 진열대 (2026-08-06 2차 — 가구 위에 아이템 진열)

배치 모드에서 제상(또는 소반·반닫이·문갑·시렁)을 놓고, 신물을 그 위로 끌어 본다

- [ ] **제상·반닫이가 다른 아이템보다 확연히 크다** (제단처럼 서는 가구 — 소109·중147·대186px)
- [ ] 신물을 가구 위로 끌면 **금색 링(진열 칸)이 뜨며 탁 붙는다** (소반2·반닫이3·제상4칸)
- [ ] **제물(대추·백설기 등)도 바닥 가구 위에 얹힌다** — 끌다 놓으면 제자리에 붙어야 하고,
      저장 후 다시 들어와도 그 자리에 있어야 한다 (존 클램프 우회 — 이게 이번의 핵심 경로)
- [ ] 얹힌 아이템이 **가구 뒤로 숨지 않는다** (상판 위 물건이 가구 몸체 앞에 그려진다)
- [ ] **가구를 옮기면 위에 얹힌 것들이 함께 간다** (곁에 세워 둔 것은 안 따라간다)
- [ ] 저장 → 재진입 후에도 진열·크기·앞뒤가 그대로다

---

## 3. 사장님만 할 수 있는 것 (제가 못 함)

- [ ] **메일 환경변수 3종** — `RESEND_API_KEY` · `STORY_INBOX_EMAIL` · `STORY_MAIL_FROM`
      없으면 사연 접수는 저장되지만 알림 메일이 안 나갑니다. 접수 자체는 정상입니다.
- [ ] **힉스필드 유료 전환** — 기원굿 영상 파이프라인의 선결 조건
      (무료 10크레딧으로는 Veo 3.1 한 번도 못 돌립니다)

---

## 4. 결정 대기

### 4-1. 기원굿 4건 (이전 세션에서 넘어옴)

- [ ] 청원(유료) 가격
- [ ] 낭독 — TTS인가 성우인가
- [ ] 본거리 소재 — 17신위 각각인가, 공통 1컷 + 합성인가 (후자 권장)
- [ ] 배포 형태 — 이름이 들어가므로 공유 신중

### 4-2. 연출 B안

- [x] ~~신당 방 좌정 신위 표정 상태머신~~ → **구현·배포됨**(`8df7cdf`). 이미 있던 표정 9종을 썼습니다 — 새 그림 0장, AI 0원.
- [ ] **Rive 도입 여부** — `.riv` 파일은 Rive 에디터 산출물이라 **제가 못 만듭니다**. 도입하려면 디자이너 작업이 선결입니다. 표정 상태머신으로 이미 상당 부분을 덮었으니, Rive 는 "신위가 실제로 **움직여야** 할 때"(보행·손짓)만 남습니다.

### 4-3. 표시광고법 (미해결, 이전 세션부터)

- [ ] 랜딩 후기 10건이 전부 창작 페르소나 / 회원수 12,403은 하드코딩 증가
      실데이터 연결 또는 "예시" 명시 필요

---

## 5. 아직 검증 못 한 것 (정직하게)

- **멤버십 웹툰 회차 실경로** — 프로덕션에 회차가 0화 예고편(무료, 5컷) 하나뿐이라, 잠금 화면·서명 만료·최적화 카운트가 전부 코드와 테스트로만 덮여 있습니다. 첫 유료 회차 올릴 때 눈으로 봐야 합니다.
- **운영 화면 실제 조작** — `E2E_USER_EMAIL`/`PASSWORD`가 셸에 없고 `.env` 읽기가 금지라 로그인 흐름을 못 돌렸습니다. 라우트 존재와 게이트(307 → 로그인)까지만 확인했습니다.
- **연출이 실제로 도는지** — 헤드리스 프리뷰는 compositing을 안 해 애니메이션이 아예 안 돕니다. 그래서 A-2 계측을 만들었습니다. 2-1이 이 항목의 답입니다.

---

## 6. 다음에 손댈 때 알아야 할 함정

- **등급을 바꾸면 본문 버킷이 바뀐다** — free=`webtoon`(공개) / membership=`webtoon-locked`(비공개). 자동 이동은 안 합니다. 화면이 경고만 띄우니 **본문을 다시 올려야** 합니다.
- **멤버십 회차는 이미지 최적화를 안 탄다** — 올릴 때 줄이는 것이 유일한 방어선입니다. 운영 화면 밖(스크립트·DB 직접 입력)으로 컷을 넣으면 원본이 그대로 독자에게 갑니다.
- **웹툰 컷에 `fitWithin`을 쓰면 안 된다** — 긴 변 기준이라 2000×10000 컷의 가로가 216px로 뭉개집니다. `fitWidth`(가로 기준)를 쓰세요. 두 함수가 같은 파일에 나란히 있습니다.
- **사연 표만 service_role** — 원문과 연락처가 함께 있는 유일한 표라 어드민 RLS 정책을 **일부러 안 만들었습니다**. "어드민인데 왜 RLS로 안 되지"라며 정책을 추가하는 건 되돌리는 결정입니다.
- **React `<ViewTransition>`은 못 쓴다** — react@19.2.4 정식 빌드에 `unstable_ViewTransition`이 없습니다(experimental 전용). 다시 조사하지 마세요.
