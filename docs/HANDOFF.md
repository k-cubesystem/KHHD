# 인수인계 — 다른 기기·다른 세션에서 이어서 작업하기

> **왜 이 파일이 리포에 있나**
> 세션 체크포인트 훅(`.claude/hooks/hhd-checkpoint-save.sh`)은 `~/.claude/hhd-session-checkpoint.md`,
> 즉 **그 컴퓨터의 홈 디렉터리**에 저장한다. 모바일 앱이나 다른 기기에서 세션을 열면 그 파일이
> 아예 없다 — 맥락이 통째로 사라진다. 그래서 **리포 안에** 인수인계를 둔다. 여기 있으면
> `git clone` 한 곳 어디서나 보인다.
>
> 갱신: 큰 작업을 마치거나 기기를 옮기기 전에 이 파일을 고치고 커밋한다.

마지막 갱신: 2026-08-11(5차) · 브랜치 `claude/determined-yonath`

---

## 1. 지금 상태

**프로덕션은 `claude/determined-yonath` 브랜치에서 나간다. main 에는 신당 2.0이 없다.**
main 으로 배포하면 회귀 사고가 난다(2026-08-01에 한 번 났고 복구했다).

최근 작업(2026-08-03~04, 전부 프로덕션 반영 확인):

| 커밋      | 내용                                                                                                          |
| --------- | ------------------------------------------------------------------------------------------------------------- |
| `b1374db` | 웹툰 서명 URL — 최적화 우회 · TTL 900초 · 일괄 발급 · 모르는 등급은 잠금                                      |
| `8359a77` | 웹툰 운영 화면 `/admin/webtoon` — 회차 등록 · 본문 업로드 · 사연 회신 · 가림 되돌리기                         |
| `a777f14` | 의례 전환(A-1) + 신물 32종 확장(14 → 46종)                                                                    |
| `d78e4d8` | 연출 계측(A-2) — `?anim=1`                                                                                    |
| `2615746` | 스크롤 읽기 막대(A-3)                                                                                         |
| `8df7cdf` | 좌정 신위 표정 상태머신(B-1) + 세션 체크목록                                                                  |
| `14f1f8c` | 체크목록 갱신                                                                                                 |
| `5913100` | 웹툰 예고편 v4 (**다른 세션 작업분** — `public/webtoon.html` 한 파일)                                         |
| (2차)     | 신물 스프라이트 33장 생성·연결(이름≠이미지 해소) · **시렁(가족 선반) 시스템** · 연출 박자 변주 · 팻말 처마 등 |

**(3차 · 2026-08-09~10) 신당 무대 대격변 — 전부 프로덕션 라이브:**

| 회차                     | 내용                                                                                                                                             |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 살아있는 배경 P1+P2      | 시간대 틴트 16테마(낮=원화 동일) + 앰비언트 시범 3테마 + 달·절기 도메인(렌더 미배선). 기획 `PRD/ARCH-shrine-living-background-v1`                |
| 한 붓의 무대(통짜)       | 뮤럴을 살림 참조 통짜 파노라마로 재설계 — 16:9 4K·width-fit·srcset(-v3 파일명). 6테마 라이브. 기획 `PLAN-stage-harmony-v1`(추기 1~6이 함정 모음) |
| 고정 살림 조절           | 신위 무대·가족 선반장·의식각을 꾸미기 드래그로 조절(`shrines.fixture_offsets`)                                                                   |
| 웅장한 틀 v4.1+접지 수복 | 닫집 제단 3테마(반가·달집·설빛, 감실에 신위)·마루 27%(마루선 y73)·앵커 2열 5점·세로 기준 렌더. 진열 아이템 동반 이동 SQL 적용                    |
| 스필 제거                | 살림 4종 크로마키 황록 스필 픽셀 수술 + despill 파이프라인 opt-in                                                                                |

**(4차 · 2026-08-10~11) 전면 확장 v5 — 16테마 완전 동등화, 프로덕션 라이브** (`9346533`+`f0d5775`):

| 항목          | 내용                                                                                                                             |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 틀 16종       | 닫집 제단(감실에 신위 봉안)을 전 테마로 확산 — 테마별 재질(옻칠·무쇠·나전·백목…). 시범 3장은 AR 고정 역산으로 바이트 무변경      |
| 마루 1/3 접지 | 접지선 = 마루선 + 바닥밴드/3 = y82(파생식 — 마루 이동 시 공짜). 밴드 75/27·마루선 73·신수 72 불변                                |
| 뮤럴 디테일   | 16:9 4K 통짜 파노라마에 디테일 보강. 시드 16/16 검증·진열 이동 6건                                                               |
| 배치 UX 개편  | **자석 스냅 전면 제거**(anchor_id 신규 발급 중단)·가구 동반 이동 제거·SIZE_PX +25%·제단 배율 0.60·선반 축소는 좌표 판정으로 전환 |

🔴 **함정**: ①public 자산 교체는 **반드시 파일명 버전업**(같은 이름 덮어쓰기 = 폰 캐시가 옛 그림 재사용, 2026-08-10 실증) ②배포 순서 «코드 → 자산 → 시드»(404는 onError가 삼켜 빈 방) ③틀 랜드마크는 육안 눈금판이 정본(자동 검출 불신) ④제단 z 밴드는 동결+틀 스위치(존 파생으로 바꾸면 13테마 재배열 사고) ⑤배치 데이터 이동 SQL 은 «앵커 id+정확일치»만(박스 판정은 겹침 이중 이동).
상태: **16테마 전부 라이브 · CEO 실기기 검수 대기**(반려 시 해당 테마만 어휘 처방 재생성). 상세 이력·함정 전체는 `PLAN-stage-harmony-v1`(추기 1~7).

**(5차 · 2026-08-11 오후) 오퍼스 6기 병렬 파도 — 살아있는 배경 완성 + 싱잉볼 BGM** (`bcdf515`~`3bf5a04`):

| 항목              | 내용                                                                                                                                       |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 앰비언트 16테마   | `AMBIENT_THEMES`(구 PILOT) 16종 확산. 확산 중 실사고 2건 수복(샘굿 파티클 0알 — 스펙-렌더 전수 대조 테스트 신설 / 도깨비 안개색)           |
| 달빛·절기(P3)     | 달=원반 아닌 **빛**(뮤럴 16장 전부 실내·크롭 13%p 편차 때문), 절기 5창 무료 연출+현판. 게이트 `AMBIENT_OMENS_V1`. 다음 창 **추석 9/24~26** |
| 싱잉볼 BGM 16트랙 | 코드 합성·60초 무이음 루프·총 11MB·결정론. 자동재생 거절 시 합성음 선점 버그 수복. 교체 시 `bgm-{theme}-v2.mp3` 파일명 버전업 필수         |
| 정비              | 죽은 대시보드 뷰 3종 -522줄(잔여 5파일 후속) · AmbientVideo는 신당 룸만 은퇴(룸 밖 9곳 살아 있음 — **컴포넌트 삭제 금지**)                 |
| 문서              | 복채 선불업 조사(`docs/REPORTS/RESEARCH-20260811-bok-prepaid-law.md`) · 가족 선반 재기획(`PLAN-family-shelf-v2.md`, B안 구현 GO 대기)      |

⚠️ 인증 콘솔 이슈 2건(코드 아님): **이메일 가입 사실상 마비 가능성**(SMTP 미설정+확인메일 필수+발송한도 전역 2통/h — OAuth만 생존) · CAPTCHA 미설정(비밀번호 무차별 대입 미방어). Supabase 콘솔 작업 대기.

게이트 기준선(2026-08-11 5차 갱신): **tsc 0 · jest 2,105 · build ✓ · CSS 게이트 87종/76종**
이 숫자보다 낮아지면 뭔가 깨진 것이다.
(eslint 는 강제 게이트가 아니다 — react-compiler 계열 기존 지적 23건이 옛 파일에 남아 있고,
새로 만지는 파일에서 0 을 유지하는 것이 규율이다. MEMORY `project_lint_gate` 참조.)

---

## 2. 모바일(또는 다른 기기)에서 **되는 것 / 안 되는 것**

### 되는 것

- 코드 읽기·수정·커밋·`git push`
- `npm run build` · `npx jest` · `npx tsc --noEmit` · `npx eslint`
- `node scripts/check-animation-css.mjs` (빌드 이후)
- Supabase MCP 로 DB 조회·마이그레이션 (`mcp__supabase__*`)

### 안 되는 것 — 그리고 그 이유

- 🔴 **프로덕션 배포(`vercel deploy --prod`)**
  `.vercel/`이 gitignore 라 프로젝트 연결 정보가 리포에 없고, Vercel CLI 로그인도 그 컴퓨터에만 있다.
  → 모바일에서는 **push 까지만** 하고, 배포는 데스크톱에서 한다.
  → 데스크톱 복귀 시: 워크트리에서 브랜치 확인 후 `vercel deploy --prod --yes` 한 줄로.

- 🔴 **로그인이 필요한 화면의 e2e 확인**
  `E2E_USER_EMAIL`/`PASSWORD` 가 셸에 없고 `.env*` 읽기는 금지다. 어느 기기에서도 못 한다.
  → 사람이 폰으로 직접 보는 수밖에 없다. 그래서 `?anim=1` 계측을 만들었다.

- ⚠️ **`~/.gstack/`, `~/.claude/hhd-session-checkpoint.md`**
  전부 기기 로컬이다. 다른 기기에서는 없다 — 이 파일이 그 대체물이다.

---

## 3. 재개 절차

1. `git pull` — 다른 세션이 커밋했을 수 있다(실제로 자주 그런다)
2. **이 파일**과 `docs/REPORTS/CHECKLIST-20260803-session.md` 를 읽는다
3. `MEMORY/MEMORY.md` 의 ADR 001~003 과 「알려진 이슈」 표를 훑는다
4. 게이트를 한 번 돌려 기준선(§1)과 맞는지 본다

```bash
npx tsc --noEmit && npx jest --silent && npm run build && node scripts/check-animation-css.mjs
```

---

## 4. 바로 이어서 할 수 있는 일

우선순위 순. 상세는 체크목록 문서에 있다.

1. **사람이 봐야 끝나는 것** — **16테마 실기기 검수(v5 틀·뮤럴, 최우선)** · `?anim=1` 연출 계측 · 운영 화면 실조작.
   제가 못 하는 항목이라 결과를 받아야 다음이 정해진다.
2. **살아있는 배경 후속** — P3 렌더 배선(달 위상·절기, **추석 9/25 전**) · 앰비언트 13테마 확산
   (`AMBIENT_PILOT_THEMES` 1줄) · P4 영험한 밤(보상 경제 결정 필요). 기획 `PRD/ARCH-shrine-living-background-v1`.
3. **연출 B안 나머지** — Rive 도입 여부(디자이너 선결) 또는 표정 상태머신 확장.
   지금 규칙은 둘뿐이다(기도 올림 → `bless` / 시작해 놓고 안 옴 → `sad`). 근거가 생기면 늘린다.
4. **웹툰 첫 유료 회차** — 멤버십 경로가 아직 한 번도 실기기 검증된 적이 없다.
   프로덕션에 회차가 0화 예고편(무료, 5컷) 하나뿐이다.
5. **표시광고법** — 랜딩 후기·회원수 (미해결, 사용자 결정 대기)

---

## 5. 손대기 전에 알아야 할 함정

- **웹툰 등급을 바꾸면 본문 버킷이 바뀐다** — free=`webtoon`(공개) / membership=`webtoon-locked`(비공개).
  자동 이동은 **안 한다**. 화면이 경고만 띄우니 본문을 다시 올려야 한다.
- **멤버십 회차는 이미지 최적화를 안 탄다**(서명 주소라 캐시가 안 맞는다). 올릴 때 줄이는 것이
  유일한 방어선이다 — 운영 화면 밖으로 컷을 넣으면 원본이 그대로 독자에게 간다.
- **웹툰 컷에 `fitWithin` 을 쓰면 안 된다.** 긴 변 기준이라 2000×10000 컷의 가로가 216px 로
  뭉개진다. `fitWidth`(가로 기준)를 쓴다. 두 함수가 같은 파일에 나란히 있다.
- **사연 표만 service_role 이다.** 원문과 연락처가 함께 있는 유일한 표라 어드민 RLS 정책을
  **일부러 안 만들었다**. "어드민인데 왜 RLS 로 안 되지"라며 정책을 추가하는 건 되돌리는 결정이다.
- **React `<ViewTransition>` 은 못 쓴다.** react@19.2.4 정식 빌드에 `unstable_ViewTransition` 이
  없다(experimental 전용). 다시 조사하지 말 것.
- **styled-jsx 금지.** App Router 에서 빌드 CSS 에 한 줄도 안 실려 연출이 무증상으로 죽는다.
  연출 CSS 는 `app/shrine-scene.css` · `app/globals.css` 정적 파일에만 둔다.
- **Bash 의 cwd 는 명령 사이에 리셋된다.** git·vercel 은 반드시 `cd <경로> && 명령` 한 줄로.
  이걸 어겨서 main 에서 prod 배포가 나간 사고가 있었다.
