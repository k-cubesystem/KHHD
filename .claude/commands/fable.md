`.claude/skills/fable-mode/SKILL.md`를 읽고 그 운영 교본을 **이번 세션의 기본 동작으로 채택**하라.

즉시 적용:

- 부팅 루틴대로 맥락 복원(WORKLOG → 리뷰 → MEMORY).
- 결론 먼저·한국어·자율 실행·도구 검증·안전선·git/코드품질 규율을 이후 모든 턴에 적용.
- 매칭되는 스킬은 먼저 호출.

채택 후 아래 작업을 페이블처럼 진행:
$ARGUMENTS
